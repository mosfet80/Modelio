---
phase: 01-resolved-model-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedQuery.java
  - platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedSource.java
  - platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedStep.java
  - platform.search.engine.test/src/org/modelio/platform/query/engine/ResolvedModelSmokeTest.java
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Resolved-model types compile in Java 17 and do not depend on unresolved AST names."
    - "Resolved-model types live in a non-exported (internal) package."
  artifacts:
    - "platform.search.engine/src/org/modelio/platform/query/engine/resolved/*"
    - "platform.search.engine.test/src/org/modelio/platform/query/engine/ResolvedModelSmokeTest.java"
  key_links:
    - "ResolvedQuery -> ResolvedSource + ResolvedStep pipeline structure exists (even if semantics are implemented in later phases)."
---

<objective>
Create the internal resolved query model skeleton: core query container, source abstraction, and step abstraction.

Purpose: Establish Phase 1’s internal executable model shape without changing public DTOs.
Output: New internal Java types + a smoke test proving they compile.
</objective>

<execution_context>
@.gsd/templates/phase-prompt.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/01-resolved-model-foundations/01-CONTEXT.md

Relevant source files:
@META-INF/MANIFEST.MF
@src/org/modelio/platform/query/api/model/query/Query.java
@src/org/modelio/platform/query/api/model/query/LimitsSpec.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal resolved model package + core query container</name>
  <files>
    platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedQuery.java,
    platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedSource.java
  </files>
  <action>
    Create a new internal (non-exported) package `org.modelio.platform.query.engine.resolved`.

    Add minimal, executable-shape records/interfaces:
    - `ResolvedQuery`: holds `ResolvedSource source`, `List<ResolvedStep> steps`, and a limits handle.
      - Prefer reusing the existing limits DTO in an immutable form (`FrozenLimitsSpec`) to avoid duplication.
    - `ResolvedSource`: sealed interface (or interface) with concrete variants for the phase-3 sources:
      - `AllSource` (evaluates to “all model elements” later)
      - `ProvidedSource` (evaluates to the “provided selection” later)

    Keep the model independent of the public unresolved query AST names.
    Do not touch OSGi exports in `META-INF/MANIFEST.MF`.
  </action>
  <verify>
    Build host bundle via aggregator:

    `cd platform.search.engine/build && rm -f mvn-build.log mvn-exit.txt && (source /work/modelio/alouette/toolkit/env_toolkit.sh && mvn -q -DskipTests=true install |& tee mvn-build.log; echo $? > mvn-exit.txt)`

    Then ensure exit code is 0:

    `cat platform.search.engine/build/mvn-exit.txt`
  </verify>
  <done>
    Internal package exists with ResolvedQuery + ResolvedSource compiling on Java 17.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define step abstraction to support later semantics</name>
  <files>
    platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedStep.java
  </files>
  <action>
    Define `ResolvedStep` as a sealed interface (or interface) and add placeholders as *types* (not logic) for the known step categories:
    - `FilterStep`
    - `TraverseStep`
    - `UnionStep`
    - `RepeatStep`

    The step types should be shaped so they can later carry resolved references (kinds/edges/properties) without referencing unresolved names.

    Keep the surface area minimal: only fields that are clearly needed in Phase 1.
  </action>
  <verify>
    Same build verification as Task 1 (aggregator build succeeds).
  </verify>
  <done>
    ResolvedStep compiles and can be used as a pipeline element in ResolvedQuery.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add a smoke test validating compilation + non-exported package intent</name>
  <files>
    platform.search.engine.test/src/org/modelio/platform/query/engine/ResolvedModelSmokeTest.java
  </files>
  <action>
    Add a minimal JUnit test that:
    - Instantiates a `ResolvedQuery` with a `ProvidedSource` and an empty steps list
    - Asserts basic invariants (non-null fields)

    The test acts as a guardrail that the internal model is consumable by execution code.
  </action>
  <verify>
    Run only this test bundle (from its aggregator):

    `cd platform.search.engine.test/build && rm -f mvn-build.log mvn-exit.txt && (source /work/modelio/alouette/toolkit/env_toolkit.sh && mvn -q -DskipTests=false -Dtest='*ResolvedModelSmokeTest' verify |& tee mvn-build.log; echo $? > mvn-exit.txt)`

    Ensure exit code is 0:

    `cat platform.search.engine.test/build/mvn-exit.txt`
  </verify>
  <done>
    Smoke test passes and provides a stable compilation/use-site for the resolved model.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Host bundle builds (`platform.search.engine/build` aggregator) with no new warnings/errors
- [ ] Test bundle runs smoke test successfully (`platform.search.engine.test/build` aggregator)
- [ ] `META-INF/MANIFEST.MF` exports are unchanged
</verification>

<success_criteria>

- Core internal resolved model skeleton exists (query/source/step) and compiles.
- No public API packages were modified or exported for the resolved model.
</success_criteria>

<output>
After completion, create `.gsd/phases/01-resolved-model-foundations/01-01-SUMMARY.md`
</output>
