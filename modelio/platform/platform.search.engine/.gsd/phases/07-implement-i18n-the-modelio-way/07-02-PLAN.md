---
phase: 07-implement-i18n-the-modelio-way
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/org/modelio/platform/query/internal/resolver/KindResolver.java
  - src/org/modelio/platform/query/internal/resolver/EdgeResolver.java
  - src/org/modelio/platform/query/internal/resolver/PropertyKeyResolver.java
  - src/org/modelio/platform/query/internal/resolver/QueryResolver.java
  - src/org/modelio/platform/query/internal/executor/SourceEvaluator.java
  - src/org/modelio/platform/query/internal/executor/ExecutionBudget.java
  - src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java
  - src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java
  # test repo (platform.search.engine.test) — 3 test files need assertion updates:
  - ../platform.search.engine.test/src/org/modelio/platform/query/resolver/KindResolverTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/resolver/EdgeResolverTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/resolver/PropertyKeyResolverTest.java
autonomous: true

must_haves:
  truths:
    - "No hardcoded diagnostic strings remain in resolver or executor packages"
    - "All diagnostic messages go through SearchEngineI18N.msg(key, args)"
    - "Tests pass: null-safe fallback keeps test suite green without OSGi activation"
    - "Build passes with no compilation errors"
  artifacts:
    - path: "src/org/modelio/platform/query/internal/resolver/KindResolver.java"
      provides: "Kind resolution diagnostics via I18N"
      contains: "SearchEngineI18N.msg"
    - path: "src/org/modelio/platform/query/internal/executor/ExecutionBudget.java"
      provides: "Budget truncation diagnostics via I18N"
      contains: "SearchEngineI18N.msg"
    - path: "src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java"
      provides: "Cycle detection diagnostics via I18N"
      contains: "SearchEngineI18N.msg"
  key_links:
    - from: "KindResolver.java / EdgeResolver.java / PropertyKeyResolver.java / QueryResolver.java"
      to: "SearchEngineI18N.msg()"
      via: "import org.modelio.platform.query.internal.SearchEngineI18N"
      pattern: "SearchEngineI18N\\.msg"
    - from: "SourceEvaluator.java / ExecutionBudget.java / RepeatStepExecutor.java"
      to: "SearchEngineI18N.msg()"
      via: "import org.modelio.platform.query.internal.SearchEngineI18N"
      pattern: "SearchEngineI18N\\.msg"
---

<objective>
Replace all hardcoded diagnostic strings in the resolver and executor packages with calls to `SearchEngineI18N.msg(key, args)`.

Purpose: Completes the I18n migration. After this plan every user-visible diagnostic message is externalized and localizable.
Output: 8 Java files updated; build and test suite green.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/phases/07-implement-i18n-the-modelio-way/07-RESEARCH.md
@.gsd/phases/07-implement-i18n-the-modelio-way/07-01-SUMMARY.md

src/org/modelio/platform/query/internal/SearchEngineI18N.java
src/org/modelio/platform/query/internal/resolver/KindResolver.java
src/org/modelio/platform/query/internal/resolver/EdgeResolver.java
src/org/modelio/platform/query/internal/resolver/PropertyKeyResolver.java
src/org/modelio/platform/query/internal/resolver/QueryResolver.java
src/org/modelio/platform/query/internal/executor/SourceEvaluator.java
src/org/modelio/platform/query/internal/executor/ExecutionBudget.java
src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java
src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate resolver diagnostic strings (4 files)</name>
  <files>
    src/org/modelio/platform/query/internal/resolver/KindResolver.java
    src/org/modelio/platform/query/internal/resolver/EdgeResolver.java
    src/org/modelio/platform/query/internal/resolver/PropertyKeyResolver.java
    src/org/modelio/platform/query/internal/resolver/QueryResolver.java
  </files>
  <action>
Add `import org.modelio.platform.query.internal.SearchEngineI18N;` to each file that does not already have it.

Then perform the following string replacements. Read each file first to verify the exact surrounding code context before editing.

**KindResolver.java** — 3 replacements:

| Location | Before | After |
|---|---|---|
| UNKNOWN_KIND blank | `"Unknown kind (blank token)"` | `SearchEngineI18N.msg("MQL.resolver.kind.unknown.blank")` |
| UNKNOWN_KIND token | `"Unknown kind '" + token + "'"` | `SearchEngineI18N.msg("MQL.resolver.kind.unknown", token)` |
| AMBIGUOUS_KIND | `"Ambiguous kind '" + token + "' resolved to " + count + ...` (full concatenation to end of string) | `SearchEngineI18N.msg("MQL.resolver.kind.ambiguous", token, Integer.valueOf(count))` |

**EdgeResolver.java** — 3 replacements:

| Location | Before | After |
|---|---|---|
| UNKNOWN_EDGE blank | `"Unknown edge (blank token)"` | `SearchEngineI18N.msg("MQL.resolver.edge.unknown.blank")` |
| UNKNOWN_EDGE token | `"Unknown edge '" + token + "'"` | `SearchEngineI18N.msg("MQL.resolver.edge.unknown", token)` |
| AMBIGUOUS_EDGE | `"Ambiguous edge '" + token + "' resolved to " + count + ...` (full concatenation) | `SearchEngineI18N.msg("MQL.resolver.edge.ambiguous", token, Integer.valueOf(count))` |

**PropertyKeyResolver.java** — 3 replacements:

| Location | Before | After |
|---|---|---|
| UNKNOWN_PROPERTY blank | `"Unknown property key (blank token)"` | `SearchEngineI18N.msg("MQL.resolver.property.unknown.blank")` |
| UNKNOWN_PROPERTY token | `"Unknown property key '" + token + "'"` | `SearchEngineI18N.msg("MQL.resolver.property.unknown", token)` |
| AMBIGUOUS_PROPERTY | full concatenation starting `"Ambiguous property key '"` | `SearchEngineI18N.msg("MQL.resolver.property.ambiguous", token, Integer.valueOf(count))` |

**QueryResolver.java** — 1 replacement:

| Location | Before | After |
|---|---|---|
| UNSUPPORTED_SOURCE | `"Unsupported source type: " + source.getClass().getName()` | `SearchEngineI18N.msg("MQL.resolver.source.unsupported", source.getClass().getName())` |

**Important**: Do NOT modify `DiagnosticCode.description()` methods — those are API-level constants and must not be changed.
Do NOT modify any `@objid` annotations.
  </action>
  <verify>
    `grep -rn "\"Unknown kind\|\"Ambiguous kind\|\"Unknown edge\|\"Ambiguous edge\|\"Unknown property\|\"Ambiguous property\|\"Unsupported source" src/org/modelio/platform/query/internal/resolver/` — must return 0 matches (no hardcoded strings remain).
    `grep -rn "SearchEngineI18N.msg" src/org/modelio/platform/query/internal/resolver/` — at least 10 matches across the 4 files.
  </verify>
  <done>All 10 resolver diagnostic strings replaced with SearchEngineI18N.msg() calls. No hardcoded diagnostic strings remain in the resolver package.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate executor diagnostic strings (4 files)</name>
  <files>
    src/org/modelio/platform/query/internal/executor/SourceEvaluator.java
    src/org/modelio/platform/query/internal/executor/ExecutionBudget.java
    src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java
    src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java
  </files>
  <action>
Add `import org.modelio.platform.query.internal.SearchEngineI18N;` to each file that needs it.

Read each file first to see the exact context, then apply these replacements:

**SourceEvaluator.java** — 2 replacements (same pattern at two call sites):

| Before | After |
|---|---|
| `String.format("Skipped %d invalid/deleted element(s)", Integer.valueOf(skippedElements.size()))` (both occurrences) | `SearchEngineI18N.msg("MQL.executor.element.skipped", Integer.valueOf(skippedElements.size()))` |

Remove the `String.format` call entirely — `SearchEngineI18N.msg` handles formatting via MessageFormat internally.

**ExecutionBudget.java** — 2 replacements:

| Before | After |
|---|---|
| `"Execution truncated: timeout (%dms) exceeded after %dms"` + `String.format` wrapping | `SearchEngineI18N.msg("MQL.executor.truncated.timeout", Long.valueOf(this.timeoutMs), Long.valueOf(elapsed))` |
| `"Execution truncated: result limit (%d) reached"` + `String.format` wrapping | `SearchEngineI18N.msg("MQL.executor.truncated.limit", Integer.valueOf(this.limit))` |

Read the file first to get exact variable names (`elapsed`, `this.timeoutMs`, `this.limit`). Replace the full `String.format(...)` expression.

**StepPipelineExecutor.java** — scan for any diagnostic strings (not LOG.warning calls) and migrate them. The LOG.warning for `"UnsupportedStep encountered"` is a debug log, not a user-facing diagnostic — leave it as-is. Only migrate strings passed to `Diagnostics.warning(...)` or `Diagnostics.error(...)` constructors.

If no diagnostic string literals exist (only LOG.warning), this file requires no changes beyond the import (skip the import too if no changes are needed).

**RepeatStepExecutor.java** — 1 replacement:

| Before | After |
|---|---|
| `"Cycle detected during repeat traversal: node '" + node.getName() + "' already visited"` (verify exact literal from file) | `SearchEngineI18N.msg("MQL.executor.cycle.detected", node.getName())` |

Read the file first to confirm the exact string literal.
  </action>
  <verify>
    `grep -rn "String.format.*Skipped\|String.format.*truncated\|String.format.*Cycle\|\"Cycle detected\|\"Execution truncated\|\"Skipped" src/org/modelio/platform/query/internal/executor/` — must return 0 matches.
    `grep -rn "SearchEngineI18N.msg" src/org/modelio/platform/query/internal/executor/` — at least 5 matches across SourceEvaluator, ExecutionBudget, RepeatStepExecutor.
  </verify>
  <done>All diagnostic strings in the executor package replaced with SearchEngineI18N.msg() calls. No hardcoded diagnostic strings remain.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification and test run</name>
  <files></files>
  <action>
Run the full build-and-test cycle:

```bash
cd /work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine && build/build.sh
```

If the build fails:
1. Check `build/mvn-build.log` for compilation errors.
2. Most likely causes:
   - Missing import for `SearchEngineI18N` in a migrated file — add the import.
   - MessageFormat argument type mismatch — ensure numeric args are boxed (`Integer.valueOf`, `Long.valueOf`).
   - `SearchEngineI18N.msg` used with wrong number of args vs. properties file placeholder count — verify key arity.
3. Fix the error and re-run: `build/build.sh`.

The tests in `platform.search.engine.test` must all pass. Since `SearchEngineI18N.msg()` falls back to raw key / MessageFormat when `SearchEngine.I18N` is null (test context), diagnostic string assertions in tests may need updating: tests that check `diagnostic.description().equals("Unknown kind 'Foo'")` will now receive `"MQL.resolver.kind.unknown"` as fallback. Read the test file to check what assertions exist and update them to match the fallback form if needed.

**Test assertion updates** — the following 3 test files in `platform.search.engine.test` have `.contains()` or `.isEqualTo()` assertions on human-readable diagnostic strings that will fail after migration (fallback returns the key):

- `/work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine.test/src/org/modelio/platform/query/resolver/KindResolverTest.java` — assertions on `"Unknown kind"`, `"Ambiguous kind"`
- `/work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine.test/src/org/modelio/platform/query/resolver/EdgeResolverTest.java` — assertions on `"Unknown edge"`
- `/work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine.test/src/org/modelio/platform/query/resolver/PropertyKeyResolverTest.java` — assertions on `"Unknown property key"`

Read each test file first to confirm which assertions exist. Then update them to check the I18N key instead:
- Before: `assertThat(diag.description()).isEqualTo("Unknown kind 'Foo'")` or `.contains("Unknown kind")`
- After: `assertThat(diag.description()).isEqualTo("MQL.resolver.kind.unknown")` or `.contains("MQL.resolver.kind")`

Use the `SearchEngineI18N` fallback contract: when `SearchEngine.I18N` is null (test context with no args), the fallback returns the raw key; with args it applies `MessageFormat.format(key, args)`. So for keys with `{0}` placeholders:
- `SearchEngineI18N.msg("MQL.resolver.kind.unknown", "Foo")` → `"MQL.resolver.kind.unknown"` (no — wait: MessageFormat.format("MQL.resolver.kind.unknown''{0}''", "Foo")` would return the formatted result)

Actually re-read the `SearchEngineI18N.msg` fallback: when I18N is null and args are provided, it calls `MessageFormat.format(key, args)` where `key` is the raw property key string (not the pattern). So the returned string IS just the key name. Update test assertions to `.contains("MQL.resolver.kind")` or `.isEqualTo("MQL.resolver.kind.unknown")` as appropriate.

Run tests after updating: `cd /work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine.test && build/run-tests.sh`
  </action>
  <verify>
    Step 1 — Build: `cat /work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine/build/mvn-exit.txt` — must contain `0`.
    Step 2 — Test suite: `cd /work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine.test && build/run-tests.sh` — must report 0 failures.
    `cat /work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine.test/build/mvn-exit.txt` — must contain `0`.
  </verify>
  <done>Build exits 0. All tests pass in both build and test-suite run. I18n migration complete.</done>
</task>

</tasks>

<verification>
Final state check:
```bash
# No hardcoded diagnostic strings remain in internal packages
grep -rn "\"Unknown kind\|\"Unknown edge\|\"Unknown property\|\"Unsupported source\|\"Cycle detected\|String.format.*Skipped\|String.format.*truncated" \
  src/org/modelio/platform/query/internal/ | grep -v ".svn" | grep -v SearchEngineI18N

# All expected I18N calls present
grep -rn "SearchEngineI18N.msg" src/org/modelio/platform/query/internal/ | grep -v ".svn" | wc -l
# Should be >= 12 (10 resolver + 2+ executor)

# Build green
cat build/mvn-exit.txt
```
</verification>

<success_criteria>
- Zero hardcoded diagnostic string literals in resolver/ and executor/ packages
- `grep -c "SearchEngineI18N.msg" src/...` ≥ 12 across the 8 migrated files
- `build/mvn-exit.txt` contains `0`
- All existing tests pass
</success_criteria>

<output>
After completion, create `.gsd/phases/07-implement-i18n-the-modelio-way/07-02-SUMMARY.md`
</output>
