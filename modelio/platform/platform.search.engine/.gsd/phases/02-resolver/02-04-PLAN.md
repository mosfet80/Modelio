---
phase: 02-resolver
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - platform.search.engine/src/org/modelio/platform/query/engine/resolver/KindResolver.java
  - platform.search.engine/src/org/modelio/platform/query/engine/resolver/EdgeResolver.java
  - platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/KindResolverTest.java
  - platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/EdgeResolverTest.java
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Ambiguous kind diagnostics list retained alternatives (metaclass/stereotype) instead of only a count."
    - "Ambiguous edge diagnostics list retained alternatives (dependency vs link-object + stereotype) instead of only a count."
    - "Tests assert ambiguity diagnostics include retained alternatives for kind and edge resolution."
  artifacts:
    - "platform.search.engine/src/org/modelio/platform/query/engine/resolver/KindResolver.java"
    - "platform.search.engine/src/org/modelio/platform/query/engine/resolver/EdgeResolver.java"
    - "platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/KindResolverTest.java"
    - "platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/EdgeResolverTest.java"
  key_links:
    - "KindResolver builds diagnostics that enumerate the retained alternatives used in ResolvedKindRef."
    - "EdgeResolver builds diagnostics that enumerate dependency and link alternatives used in ResolvedEdgeRef."
---

<objective>
Close Phase-2 verification gaps by listing retained alternatives in ambiguity diagnostics for kind and edge resolution.

Purpose: Make ambiguity diagnostics actionable and match phase success criteria.
Output: Updated diagnostics formatting + tests asserting alternative listings.
</objective>

<execution_context>
@.gsd/templates/phase-prompt.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/02-resolver/02-CONTEXT.md
@.gsd/phases/02-resolver/02-resolver-VERIFICATION.md

Relevant source files:
@src/org/modelio/platform/query/engine/resolver/KindResolver.java
@src/org/modelio/platform/query/engine/resolver/EdgeResolver.java
@src/org/modelio/platform/query/engine/diagnostics/Diagnostics.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retained alternatives listing to kind ambiguity diagnostics</name>
  <files>
    platform.search.engine/src/org/modelio/platform/query/engine/resolver/KindResolver.java
  </files>
  <action>
    Extend KindResolver ambiguity diagnostics to include the retained alternatives:

    - Keep existing wording for unknown/blank tokens.
    - For ambiguity, append a deterministic list of alternatives.
    - Use a stable, human-readable representation (e.g., "metaclass=Standard.Class" and "stereotype=Module::Name[base=Class]").
    - Keep ordering aligned with alternatives: metaclass first, then stereotypes ordered by UUID/name/base.

    Do not change resolution behavior; only diagnostic content.
  </action>
  <verify>
    Existing KindResolver unit tests updated in Task 3 pass.
  </verify>
  <done>
    Ambiguous kind diagnostics include explicit alternatives.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add retained alternatives listing to edge ambiguity diagnostics</name>
  <files>
    platform.search.engine/src/org/modelio/platform/query/engine/resolver/EdgeResolver.java
  </files>
  <action>
    Extend EdgeResolver ambiguity diagnostics to include the retained alternatives:

    - Keep existing wording for unknown/blank tokens.
    - For ambiguity, append a deterministic list of alternatives.
    - Include dependency edges and link-object edges distinctly (e.g., "dependency=Source::edge->Target" and "link=Standard.Link" or "link=Standard.Link[stereotype=Module::Name]").
    - Preserve the current alternative ordering: dependencies first, then links, with stable sorting.

    Do not change resolution behavior; only diagnostic content.
  </action>
  <verify>
    Existing EdgeResolver unit tests updated in Task 3 pass.
  </verify>
  <done>
    Ambiguous edge diagnostics include explicit alternatives.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update resolver tests to assert ambiguity diagnostics list alternatives</name>
  <files>
    platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/KindResolverTest.java,
    platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/EdgeResolverTest.java
  </files>
  <action>
    Extend ambiguity tests to assert that diagnostic messages include retained alternatives:

    - KindResolverTest: confirm diagnostic contains both metaclass and stereotype identifiers for the ambiguous token.
    - EdgeResolverTest: confirm diagnostic contains dependency and link identifiers for the ambiguous token.

    Focus on substring checks to avoid over-coupling to full message formatting, but ensure alternative listing is present.
  </action>
  <verify>
    Run test bundle aggregator:

    `cd platform.search.engine.test/build && rm -f mvn-build.log mvn-exit.txt && (source /work/modelio/alouette/toolkit/env_toolkit.sh && mvn -q -DskipTests=false -Dtest='*KindResolverTest,*EdgeResolverTest' verify |& tee mvn-build.log; echo $? > mvn-exit.txt)`

    Ensure exit code is 0:

    `cat platform.search.engine.test/build/mvn-exit.txt`
  </verify>
  <done>
    Tests assert ambiguity diagnostics list retained alternatives and pass.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Host bundle builds (`platform.search.engine/build` aggregator)
- [ ] Kind/edge resolver tests pass with ambiguity listing assertions
- [ ] Diagnostics list retained alternatives for kind and edge ambiguity cases
</verification>

<success_criteria>

- Ambiguous kind diagnostics list metaclass/stereotype alternatives.
- Ambiguous edge diagnostics list dependency/link alternatives.
- Tests lock in diagnostics listing behavior.
</success_criteria>

<output>
After completion, create `.gsd/phases/02-resolver/02-04-SUMMARY.md`
</output>
