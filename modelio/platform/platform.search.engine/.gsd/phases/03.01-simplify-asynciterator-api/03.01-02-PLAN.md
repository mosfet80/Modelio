---
phase: 03.01-simplify-asynciterator-api
plan: 02
type: execute
wave: 2
depends_on: ["03.01-01"]
files_modified:
  - src/org/modelio/platform/query/internal/executor/BufferedAsyncIterator.java
  - src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/BufferedAsyncIteratorTest.java
autonomous: true
must_haves:
  truths:
    - "Concurrent nextAsync pulls on the same iterator fail fast instead of racing silently."
    - "After terminal completion, extra pulls are treated as misuse and fail fast."
    - "Dispose/cancel while pull is pending resolves pending pull as Optional.empty()."
  artifacts:
    - path: "src/org/modelio/platform/query/internal/executor/BufferedAsyncIterator.java"
      provides: "Thread-safe nextAsync lifecycle with fail-fast guards"
      contains: "nextAsync"
    - path: "../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/BufferedAsyncIteratorTest.java"
      provides: "Behavioral coverage for fail-fast and pending-cancel semantics"
      contains: "concurrent"
    - path: "src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java"
      provides: "Producer interaction with updated iterator pull semantics"
      contains: "Optional"
  key_links:
    - from: "BufferedAsyncIterator"
      to: "StreamingQueryResponse"
      via: "items/diagnostics stream buffers"
      pattern: "BufferedAsyncIterator"
---

<objective>
Rework BufferedAsyncIterator internals for robust single-pull semantics and misuse detection.

Purpose: reduce race-condition risk and make async stream lifecycle predictable under concurrent usage.
Output: hardened buffered iterator implementation with updated producer integration and focused tests.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-CONTEXT.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-01-SUMMARY.md
@.gsd/phases/03-executor-foundations/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-implement BufferedAsyncIterator around nextAsync Optional semantics</name>
  <files>
    src/org/modelio/platform/query/internal/executor/BufferedAsyncIterator.java
  </files>
  <action>
    Replace legacy null-based API implementation with Optional-based single pull flow:
    - Implement `nextAsync(): CompletableFuture&lt;Optional&lt;T&gt;&gt;`.
    - Preserve FIFO order and completion/disposal idempotency.
    - Ensure pending pull unblocks on complete/dispose with `Optional.empty()`.
    - Remove obsolete methods and dead code (`drainToPending`, null-return logic).
    - Preserve original interruption causes where failures are exceptional.
  </action>
  <verify>
    `grep -n "next\(|hasNext\(|onNext\(" src/org/modelio/platform/query/internal/executor/BufferedAsyncIterator.java` returns no legacy methods.
    `grep -n "nextAsync\|Optional" src/org/modelio/platform/query/internal/executor/BufferedAsyncIterator.java` confirms new semantics.
  </verify>
  <done>BufferedAsyncIterator exposes only nextAsync with Optional terminal behavior and no legacy code paths.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce fail-fast misuse guards for concurrent and post-terminal pulls</name>
  <files>
    src/org/modelio/platform/query/internal/executor/BufferedAsyncIterator.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/BufferedAsyncIteratorTest.java
  </files>
  <action>
    Add explicit guardrails matching locked decisions:
    - If a second `nextAsync()` is called while a pull is pending, fail fast (exceptional future).
    - Once iterator reached terminal completion, subsequent pulls fail fast (misuse).
    - Disposal while pending returns `Optional.empty()` for the pending pull and finalizes lifecycle.
    Update/create tests to prove these exact behaviors and keep existing FIFO + completion behavior coverage.
  </action>
  <verify>
    `bash ../platform.search.engine.test/build/run-tests.sh -Dtest=BufferedAsyncIteratorTest` exits with code 0.
    Test class contains explicit assertions for concurrent call rejection and post-terminal misuse.
  </verify>
  <done>Iterator misuse is fail-fast and test-covered; pending dispose behavior remains deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Align StreamingQueryResponse producer buffering with new iterator contract</name>
  <files>
    src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  </files>
  <action>
    Adjust `StreamingQueryResponse` integration points so item/diagnostic buffers operate correctly with Optional-based pulls and fail-fast semantics.
    Keep execution behavior unchanged in this plan (state metadata is handled in the next plan).
    Ensure no usage of removed iterator methods remains.
  </action>
  <verify>
    `grep -n "onNext\(|hasNext\(|next\(" src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java` returns no AsyncIterator legacy call sites.
    `bash ./build/build.sh` succeeds.
  </verify>
  <done>StreamingQueryResponse remains functional with the reworked iterator implementation.</done>
</task>

</tasks>

<verification>
- Buffered iterator respects Optional terminal semantics and strict misuse rules.
- Pending pull cancellation behavior is deterministic and tested.
- Executor module builds cleanly after iterator internals rewrite.
</verification>

<success_criteria>
- Concurrent `nextAsync()` calls fail fast.
- Post-terminal extra pulls fail fast.
- Pending pull resolves `Optional.empty()` when disposed/cancelled.
- `BufferedAsyncIteratorTest` passes.
</success_criteria>

<output>
After completion, create `.gsd/phases/03.01-simplify-asynciterator-api/03.01-02-SUMMARY.md`
</output>
