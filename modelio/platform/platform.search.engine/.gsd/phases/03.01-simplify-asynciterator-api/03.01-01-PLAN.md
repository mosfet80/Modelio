---
phase: 03.01-simplify-asynciterator-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/org/modelio/platform/query/api/model/result/AsyncIterator.java
  - src/org/modelio/platform/query/api/model/result/AsyncIterators.java
  - src/org/modelio/platform/query/internal/executor/EmptyAsyncIterator.java
  - src/org/modelio/platform/query/internal/QueryEngine.java
  - src/org/modelio/platform/query/internal/executor/SourceEvaluator.java
autonomous: true
must_haves:
  truths:
    - "Consumers pull results through a single async entry point returning Optional values."
    - "Async iterator usage patterns are documented and reusable through helper APIs."
    - "Executor sources compile against the new iterator contract without legacy next/hasNext/onNext usage."
  artifacts:
    - path: "src/org/modelio/platform/query/api/model/result/AsyncIterator.java"
      provides: "Single pull contract: nextAsync(): CompletableFuture<Optional<T>>"
      contains: "nextAsync"
    - path: "src/org/modelio/platform/query/api/model/result/AsyncIterators.java"
      provides: "Drain helper for common consumption"
      contains: "drain"
    - path: "src/org/modelio/platform/query/internal/executor/EmptyAsyncIterator.java"
      provides: "Empty implementation aligned with Optional terminal semantics"
      contains: "nextAsync"
  key_links:
    - from: "src/org/modelio/platform/query/internal/QueryEngine.java"
      to: "org.modelio.platform.query.api.model.result.AsyncIterator"
      via: "Compilation against nextAsync-based API"
      pattern: "AsyncIterator"
---

<objective>
Define and roll out the new minimal AsyncIterator consumption contract.

Purpose: enforce a single pull API (`nextAsync`) and remove ambiguity between sync/async iterator methods.
Output: updated public iterator API, helper utilities, and executor production sources compiling with the new contract.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-CONTEXT.md
@.gsd/phases/03-executor-foundations/03-01-SUMMARY.md
@.gsd/phases/03-executor-foundations/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AsyncIterator public contract with single pull API</name>
  <files>
    src/org/modelio/platform/query/api/model/result/AsyncIterator.java
  </files>
  <action>
    Hard-break the API exactly as locked in phase context:
    - Remove `next()`, `hasNext()`, and `onNext()` from the contract.
    - Introduce `CompletableFuture&lt;Optional&lt;T&gt;&gt; nextAsync()` as the only pull method.
    - Add `forEachRemaining(Consumer&lt;T&gt;)` convenience default method based on `nextAsync()` loop.
    - Update Javadoc to define terminal semantics (`Optional.empty()` means terminal or cancelled).
    Keep interface minimal: do not add map/filter/flatMap/peek methods.
  </action>
  <verify>
    `grep -n "next\(|hasNext\(|onNext\(" src/org/modelio/platform/query/api/model/result/AsyncIterator.java` returns no legacy API declarations.
    `grep -n "nextAsync" src/org/modelio/platform/query/api/model/result/AsyncIterator.java` returns the new method.
  </verify>
  <done>AsyncIterator exposes only nextAsync-based consumption plus forEachRemaining helper.</done>
</task>

<task type="auto">
  <name>Task 2: Add AsyncIterators helper and align EmptyAsyncIterator</name>
  <files>
    src/org/modelio/platform/query/api/model/result/AsyncIterators.java
    src/org/modelio/platform/query/internal/executor/EmptyAsyncIterator.java
  </files>
  <action>
    Create helper utility class `AsyncIterators` with static `drain(...)` APIs for both item collection and consumer callback use-cases.
    Update `EmptyAsyncIterator` to implement `nextAsync()` and return `CompletableFuture.completedFuture(Optional.empty())`.
    Remove any obsolete behavior/comments tied to legacy null-based `next/onNext` semantics.
  </action>
  <verify>
    `grep -n "class AsyncIterators|drain\(" src/org/modelio/platform/query/api/model/result/AsyncIterators.java` finds helper implementation.
    `grep -n "Optional.empty\(\)" src/org/modelio/platform/query/internal/executor/EmptyAsyncIterator.java` confirms terminal semantics.
  </verify>
  <done>Project contains a reusable AsyncIterators drain helper and EmptyAsyncIterator matches Optional-based API.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate executor production sources away from onNext/hasNext/next</name>
  <files>
    src/org/modelio/platform/query/internal/QueryEngine.java
    src/org/modelio/platform/query/internal/executor/SourceEvaluator.java
  </files>
  <action>
    Refactor production call sites in executor foundations to use `nextAsync()` and Optional flow only.
    Keep behavior unchanged (no new execution features).
    Ensure no production source in `platform.search.engine` still calls legacy iterator methods.
  </action>
  <verify>
    `grep -R "\.onNext\(|\.hasNext\(|\.next\(" src/org/modelio/platform/query/internal --include='*.java'` returns no AsyncIterator call-site usage.
    `bash ./build/build.sh` succeeds (module build green).
  </verify>
  <done>Executor production sources compile with new async pull contract and no legacy iterator API usage.</done>
</task>

</tasks>

<verification>
- Public `AsyncIterator` API is hard-broken to `nextAsync()` + helper-only usage.
- Production executor sources compile with no legacy iterator methods.
- `AsyncIterators.drain(...)` is available for predictable consumption patterns.
</verification>

<success_criteria>
- `AsyncIterator` contains a single pull method (`nextAsync`) and no `next/hasNext/onNext`.
- Empty iterator returns `Optional.empty()` terminal values.
- Build succeeds after production migration.
</success_criteria>

<output>
After completion, create `.gsd/phases/03.01-simplify-asynciterator-api/03.01-01-SUMMARY.md`
</output>
