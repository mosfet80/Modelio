---
phase: 03.01-simplify-asynciterator-api
plan: 03
type: execute
wave: 3
depends_on: ["03.01-01", "03.01-02"]
files_modified:
  - src/org/modelio/platform/query/api/model/result/IQueryResponse.java
  - src/org/modelio/platform/query/api/model/result/QueryResponseState.java
  - src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/StreamingQueryResponseTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/QueryEngineExecutionTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/SourceEvaluatorTest.java
autonomous: true
must_haves:
  truths:
    - "Response terminal reason is observable through response.state() with ACTIVE/ENDED/CANCELLED/FAILED."
    - "Item and diagnostics consumption loops rely only on nextAsync Optional protocol."
    - "Cancellation and runtime failures produce consistent state + diagnostics behavior."
    - "Runtime failure handling preserves the original exception cause (no forced wrapper exception)."
    - "This phase reuses existing diagnostic codes only and introduces no new diagnostic enum/code values."
    - "IQueryResponse owns iterator lifecycle; response dispose cascades and remains synchronous/idempotent."
  artifacts:
    - path: "src/org/modelio/platform/query/api/model/result/QueryResponseState.java"
      provides: "Terminal state model for query responses"
      contains: "ACTIVE"
    - path: "src/org/modelio/platform/query/api/model/result/IQueryResponse.java"
      provides: "state() accessor for terminal reason metadata"
      contains: "state"
    - path: "src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java"
      provides: "Thread-safe response state transitions and diagnostics consistency"
      contains: "state"
    - path: "../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/StreamingQueryResponseTest.java"
      provides: "State and cancellation behavior coverage"
      contains: "state"
  key_links:
    - from: "StreamingQueryResponse.dispose()"
      to: "IQueryResponse.state()"
      via: "ACTIVE -> CANCELLED transition"
      pattern: "CANCELLED"
    - from: "StreamingQueryResponse runtime exception handling"
      to: "diagnostics stream"
      via: "RUNTIME_ERROR diagnostic + FAILED state"
      pattern: "RUNTIME_ERROR"
    - from: "IQueryResponse.dispose()"
      to: "items()/diagnostics() iterators"
      via: "response-owned lifecycle cascade"
      pattern: "dispose"
---

<objective>
Expose response terminal state metadata and complete migration of tests/consumers to nextAsync Optional loops.

Purpose: separate end-of-stream payload semantics from terminal reason semantics, as defined in phase decisions.
Output: stateful query response API, state-aware StreamingQueryResponse, and updated targeted test suite.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-CONTEXT.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-01-SUMMARY.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-02-SUMMARY.md
@.gsd/phases/03-executor-foundations/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IQueryResponse state metadata contract</name>
  <files>
    src/org/modelio/platform/query/api/model/result/QueryResponseState.java
    src/org/modelio/platform/query/api/model/result/IQueryResponse.java
  </files>
  <action>
    Introduce response state metadata exactly per locked decisions:
    - Create `QueryResponseState` enum with `ACTIVE`, `ENDED`, `CANCELLED`, `FAILED`.
    - Add `state()` accessor to `IQueryResponse`.
    - Keep `dispose()` naming and synchronous lifecycle.
    - Keep API minimal (no extra lifecycle APIs in this phase).
    Document expected single-path transition from ACTIVE to one terminal state.
  </action>
  <verify>
    `grep -n "enum QueryResponseState\|ACTIVE\|ENDED\|CANCELLED\|FAILED" src/org/modelio/platform/query/api/model/result/QueryResponseState.java` confirms states.
    `grep -n "state\(\)" src/org/modelio/platform/query/api/model/result/IQueryResponse.java` confirms accessor.
  </verify>
  <done>IQueryResponse exposes thread-safe state metadata contract with the required four states.</done>
</task>

<task type="auto">
  <name>Task 2: Implement thread-safe state transitions in StreamingQueryResponse</name>
  <files>
    src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  </files>
  <action>
    Implement response-owned terminal state handling:
    - Maintain `ACTIVE` initial state and transition once to exactly one terminal state.
    - Set `ENDED` on normal completion, `CANCELLED` on external dispose/cancel, `FAILED` on runtime error.
    - Ensure `state()` reads are thread-safe.
    - When dispose/cancel occurs during pending pull, pending pull resolves empty and state becomes `CANCELLED`.
    - Keep diagnostics behavior consistent (warning diagnostic for cancellation using existing codes).
    - Preserve original runtime failure causes end-to-end (no forced wrapping): diagnostics and exceptional completion must retain the original cause chain.
    - Enforce reuse-only diagnostic policy in this phase: do not add new `DiagnosticCode` values; reuse existing constants.
  </action>
  <verify>
    `grep -n "QueryResponseState|state\(\)" src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java` confirms state wiring.
    `grep -n "CANCELLED\|FAILED\|ENDED" src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java` confirms transitions.
    `git diff -- src/org/modelio/platform/query/api/model/result/DiagnosticCode.java | grep -q . && exit 1 || exit 0` confirms no diagnostic code additions in this phase.
  </verify>
  <done>StreamingQueryResponse exposes correct terminal reason metadata with single-path transitions, preserves original exception causes, and keeps diagnostic code usage reuse-only.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate and verify targeted executor tests on nextAsync + state semantics</name>
  <files>
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/StreamingQueryResponseTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/QueryEngineExecutionTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/SourceEvaluatorTest.java
  </files>
  <action>
    Update tests to consume streams with `nextAsync().get(...)` returning `Optional`.
    Add/update assertions for response state transitions (`ACTIVE` -> terminal state).
    Add explicit lifecycle-ownership assertions: disposing `IQueryResponse` cascades to iterators, pending pulls resolve empty, and repeated dispose calls stay synchronous/idempotent.
    Verify disposal, interruption and runtime failure scenarios assert both diagnostics and final state.
    Add explicit failure-path assertions that original throwable causes are preserved (no forced wrapping in assertions).
    Keep diagnostic-code policy explicit in tests by asserting existing codes are used (no expectation of new code values).
    Keep query semantics unchanged; this is API/state migration only.
  </action>
  <verify>
    `bash ../platform.search.engine.test/build/run-tests.sh -Dtest=BufferedAsyncIteratorTest,StreamingQueryResponseTest,SourceEvaluatorTest,QueryEngineExecutionTest` exits with code 0.
    `grep -n "state\(|dispose\(|RUNTIME_ERROR\|INTERRUPTED\|getCause" ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/StreamingQueryResponseTest.java` confirms lifecycle/cause/code assertions are present.
    `bash ./build/build.sh` exits with code 0.
  </verify>
  <done>Targeted executor tests pass using nextAsync Optional loops, validate response-owned lifecycle disposal, and verify original cause preservation with existing diagnostic codes.</done>
</task>

</tasks>

<verification>
- Query response state metadata is available and synchronized with execution lifecycle.
- All targeted iterator/executor tests use nextAsync Optional flow.
- Build and targeted tests complete green, satisfying migration gate.
</verification>

<success_criteria>
- `state()` exists and reports ACTIVE/ENDED/CANCELLED/FAILED correctly.
- Disposal and runtime failure behavior is observable via both diagnostics and final state, with response-owned lifecycle cascade.
- Runtime failure path preserves original exception causes in diagnostics and failing futures.
- No new diagnostic enum/code definitions are introduced in this phase.
- Targeted iterator/executor tests pass with no legacy iterator API usage.
</success_criteria>

<output>
After completion, create `.gsd/phases/03.01-simplify-asynciterator-api/03.01-03-SUMMARY.md`
</output>
