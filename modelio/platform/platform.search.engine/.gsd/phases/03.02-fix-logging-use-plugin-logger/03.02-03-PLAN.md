---
phase: 03.02
plan: 03
type: execute
wave: 3
depends_on: ["03.02-02"]
files_modified: []
autonomous: true
must_haves:
  truths:
    - "Executor code contains zero System.Logger references"
    - "SearchEngine.LOG is used consistently across all executor classes"
    - "Logging behaves identically to before (same messages, levels, destinations)"
    - "Bundle-level logging configuration flows through PluginLogger"
  artifacts:
    - path: "src/org/modelio/platform/query/api/model/execution/"
      description: "All executor classes verified using SearchEngine.LOG"
    - path: "src/org/modelio/platform/search/engine.test/"
      description: "Tests verifying logging works correctly"
  key_links:
    - from: "Executor log calls"
      to: "SearchEngine.LOG"
      via: "static reference"
    - from: "SearchEngine.LOG"
      to: "PluginLogger"
      via: "initialization"
---

<objective>
Verify logging migration is complete and correct: SearchEngine.LOG is used consistently, no System.Logger remains, and logging behavior is unchanged.

Purpose: Final verification that API migration is complete and working correctly.
Output: Confidence that executor logging is now properly centralized and configured.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.02-fix-logging-use-plugin-logger/03-02-CONTEXT.md
@.gsd/phases/03.02-fix-logging-use-plugin-logger/03.02-01-SUMMARY.md
@.gsd/phases/03.02-fix-logging-use-plugin-logger/03.02-02-SUMMARY.md

# Phase Success Criteria
1. All executor log statements use SearchEngine.LOG
2. Logging is consistent and respects bundle-level config
3. No use of System.Logger or ad-hoc logging in executor code
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static code scan — confirm zero System.Logger in executor</name>
  <files>N/A (verification)</files>
  <action>
    Run comprehensive grep to confirm logger migration:
    
    1. Search entire executor path for System.Logger:
       grep -r "System\.Logger\|System\.getLogger\|System\.LoggerFinder" \
         src/org/modelio/platform/query/api/model/execution/ \
         src/org/modelio/platform/search/engine/ \
         --include="*.java"
       
       Result should be empty (0 matches) or only in comments/strings.
    
    2. Search for any remaining raw System.Logger method calls:
       grep -r "\.log(" src/org/modelio/platform/query/api/model/execution/ \
         | grep -v "LOG\\.log\|SearchEngine\\.LOG"
       
       Result should show only LOG.log() or SearchEngine.LOG.log() calls.
    
    3. Verify SearchEngine.LOG imports are present in all executor classes:
       grep -r "import.*SearchEngine.LOG\|import static.*SearchEngine" \
         src/org/modelio/platform/query/api/model/execution/ \
         | wc -l
       
       Compare with number of executor classes that log.
  </action>
  <verify>
    grep for System.Logger: 0 matches (or only in comments)
    grep for .log() calls: all match LOG. or SearchEngine.LOG pattern
    SearchEngine.LOG imports match logged executor classes
  </verify>
  <done>Static analysis confirms zero System.Logger in executor, all use SearchEngine.LOG.</done>
</task>

<task type="auto">
  <name>Task 2: Compile and verify no logging-related errors</name>
  <files>N/A (build verification)</files>
  <action>
    Build and check for any logging-related compilation issues:
    
    1. Full rebuild:
       cd /work/modelio/alouette/work/eclipse/modelio/platform/platform.search.engine
       mvn clean compile -q 2>&1 | tee /tmp/compile.log
    
    2. Check for any errors:
       grep -i "error.*log\|log.*error" /tmp/compile.log
       grep -i "cannot find symbol.*LOG\|LOG.*not found" /tmp/compile.log
    
    3. If no errors, confirm build success:
       echo $? (should be 0)
    
    4. Verify target/classes contains executor code:
       ls target/classes/org/modelio/platform/query/api/model/execution/ | wc -l
  </action>
  <verify>
    Build completes with exit code 0.
    No logging-related compilation errors.
    Executor classes are compiled (not skipped or failed).
  </verify>
  <done>Compilation successful, no logger-related build errors.</done>
</task>

<task type="auto">
  <name>Task 3: Run executor tests to verify logging behavior unchanged</name>
  <files>N/A (test execution)</files>
  <action>
    Execute tests to ensure logging migration didn't break functionality:
    
    1. Run all executor/execution tests:
       mvn test -Dtest='*Execution*,*Execute*,*Query*' -q 2>&1 | tee /tmp/test.log
    
    2. Check test results:
       grep -E "Tests run:|Failures:|Errors:" /tmp/test.log
    
    3. If any failures, investigate logging-related test failures:
       grep -B5 -A5 "AssertionError\|Exception.*Log" /tmp/test.log
    
    4. Confirm all tests pass:
       echo $? (should be 0)
  </action>
  <verify>
    All tests pass (0 failures, 0 errors).
    No test failures related to logging.
    Log output in tests appears normal (if testable).
  </verify>
  <done>All executor tests pass, logging behavior verified unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Documentation — record logging migration completion</name>
  <files>N/A (documentation)</files>
  <action>
    Create a brief summary of the migration for future reference:
    
    1. Document what was changed:
       - List of executor classes updated
       - Number of log statements migrated
       - Patterns replaced (e.g., System.Logger.log(Level.DEBUG, ...) → LOG.debug(...))
    
    2. Record any issues encountered and resolved:
       - Initialization order problems (if any)
       - API differences in PluginLogger vs System.Logger (if any)
       - Test adjustments needed (if any)
    
    3. Verify bundle logging configuration:
       - Confirm SearchEngine.LOG respects bundle activation
       - If applicable, note any logging config files that apply
    
    4. Add notes for future maintainers:
       - Do not use System.Logger directly in executor code
       - Use SearchEngine.LOG for all executor logging
       - Maintain centralization as new executor features are added
  </action>
  <verify>
    Migration summary is complete and accurate.
    Issues (if any) are documented and resolved.
    Bundle logging configuration is understood and documented.
  </verify>
  <done>Migration documentation complete, future maintainers have clear record of changes.</done>
</task>

</tasks>

<verification>
After completion:
1. Static scan confirms zero System.Logger in executor code
2. Build succeeds without logging-related errors
3. All executor tests pass
4. Logging behavior is identical to pre-migration state
5. Migration is complete and documented
6. Success criteria are met:
   - All executor log statements use SearchEngine.LOG ✓
   - Logging is consistent and respects bundle config ✓
   - No use of System.Logger in executor code ✓
</verification>

<success_criteria>
✓ Zero System.Logger references in executor path (static analysis)
✓ SearchEngine.LOG used consistently across all executor classes
✓ Build passes without errors
✓ All executor tests pass
✓ Logging behavior unchanged (same messages, levels, output)
✓ Bundle logging configuration flows through PluginLogger
✓ Migration documented for future maintenance
</success_criteria>

<output>
After completion, create `.gsd/phases/03.02-fix-logging-use-plugin-logger/03.02-03-SUMMARY.md`
</output>
