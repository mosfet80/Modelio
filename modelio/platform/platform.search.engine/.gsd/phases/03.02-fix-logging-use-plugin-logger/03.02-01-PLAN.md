---
phase: 03.02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
must_haves:
  truths:
    - "All System.Logger calls in executor path are identified and mapped"
    - "Usage patterns (debug, trace, warning, error) are catalogued"
    - "SearchEngine.LOG is confirmed available and initialized"
  artifacts:
    - path: "src/org/modelio/platform/query/api/model/execution"
      description: "Executor implementation classes (QueryEngine, evaluators)"
    - path: "src/org/modelio/platform/search/engine/plugin/SearchEngine.java"
      description: "Central logger instance"
  key_links:
    - from: "Executor classes"
      to: "SearchEngine.LOG"
      via: "import dependency"
      pattern: "import.*SearchEngine|LOG"
---

<objective>
Map all current logging usage in the executor implementation to understand what needs migration.

Purpose: Provide complete inventory of `System.Logger` calls and logging patterns before replacement phase.
Output: Detailed discovery of logging locations, patterns, and initialization state.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.02-fix-logging-use-plugin-logger/03-02-CONTEXT.md

# Phase Context: API Migration Only
- No behavior changes, only API/destination replacement
- All existing logging statements stay; only the logger changes
- SearchEngine.LOG must be centralized instance using PluginLogger
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search for System.Logger usage in executor code</name>
  <files>N/A (read-only discovery)</files>
  <action>
    Search the executor implementation for all current logging:
    
    1. Find all imports or usages of `System.Logger` or `System.getLogger`:
       grep -r "System\.Logger\|System\.getLogger" src/org/modelio/platform/query/api/model/execution/
       grep -r "System\.Logger\|System\.getLogger" src/org/modelio/platform/search/engine/
    
    2. For each file found, identify:
       - Class name
       - Logger variable name/declaration
       - All log() call sites with their log levels (debug, trace, info, warning, error)
       - Number of logging statements per file
    
    3. Cross-reference with QueryEngine, source evaluation, step execution code paths
    
    4. Document findings in a table: File | Logger Name | Log Calls Count | Levels Used | Key Message Patterns
  </action>
  <verify>
    grep output shows all System.Logger calls found (or confirms none if already migrated).
    Document findings clearly for next phase reference.
  </verify>
  <done>Complete inventory of System.Logger usage across all executor classes, ready for migration.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SearchEngine.LOG availability and PluginLogger integration</name>
  <files>src/org/modelio/platform/search/engine/plugin/SearchEngine.java</files>
  <action>
    Confirm the centralized logger is available and properly integrated:
    
    1. Locate SearchEngine.LOG static field:
       grep -n "static.*LOG\|LOG.*=.*Logger" src/org/modelio/platform/search/engine/plugin/SearchEngine.java
    
    2. Verify it is initialized using PluginLogger:
       - Check if LOG uses PluginLogger or equivalent bundle-aware logger
       - Confirm initialization is done early (in static initializer or plugin activator)
    
    3. Check if SearchEngine.LOG is:
       - Private static final (immutable)
       - Accessible via SearchEngine.LOG (public static access)
       - Properly configured for bundle-level logging
    
    4. Verify no issues with circular dependencies or early-initialization problems
  </action>
  <verify>
    Confirm SearchEngine.LOG is defined, initialized, and accessible.
    Verify it uses PluginLogger (not System.Logger).
    Check that initialization happens before executor code runs.
  </verify>
  <done>SearchEngine.LOG confirmed as centralized logger instance, ready for executor migration.</done>
</task>

<task type="auto">
  <name>Task 3: Document logging patterns and prepare replacement roadmap</name>
  <files>N/A (analysis task)</files>
  <action>
    Create a structured replacement roadmap:
    
    1. From Tasks 1-2 findings, list all executor classes that need Logger changes:
       Format: 
       Class | System.Logger Replace With | Log Calls | Pattern | Dependencies
    
    2. Identify any initialization dependencies:
       - Does executor code run before SearchEngine initialization?
       - Are there any static initializers that log?
       - Can SearchEngine.LOG be safely imported in executor classes?
    
    3. Categorize changes by complexity:
       - Simple: Single logger variable, few calls
       - Medium: Multiple loggers or complex call patterns
       - Complex: Conditional initialization or early-load concerns
    
    4. Prepare a priority order for replacement (e.g., start with simple, then medium/complex)
    
    5. Document any gotchas or edge cases found
  </action>
  <verify>
    Roadmap document is complete and ready for execution phase.
    All dependencies are identified.
    No missing information that would block replacement phase.
  </verify>
  <done>Replacement roadmap created, executor classes prioritized, next phase ready to start migration.</done>
</task>

</tasks>

<verification>
After completion:
1. Inventory of System.Logger usage is accurate and complete
2. SearchEngine.LOG verified as available and initialized
3. Replacement roadmap is detailed enough for next phase to execute without questions
4. All executor classes are catalogued and prioritized
</verification>

<success_criteria>
✓ Complete mapping of current System.Logger usage across executor
✓ SearchEngine.LOG availability and PluginLogger integration verified
✓ Replacement roadmap documented for next phase execution
✓ No blocking dependencies or initialization issues identified
</success_criteria>

<output>
After completion, create `.gsd/phases/03.02-fix-logging-use-plugin-logger/03.02-01-SUMMARY.md`
</output>
