---
phase: 03.02
plan: 02
type: execute
wave: 2
depends_on: ["03.02-01"]
files_modified: []
autonomous: true
must_haves:
  truths:
    - "All System.Logger calls have been replaced with SearchEngine.LOG"
    - "Log message content is unchanged (same strings, same levels)"
    - "All imports now reference SearchEngine.LOG, not System.Logger"
  artifacts:
    - path: "src/org/modelio/platform/query/api/model/execution/"
      description: "All executor classes using SearchEngine.LOG"
  key_links:
    - from: "Each executor class"
      to: "SearchEngine.LOG"
      via: "static import"
      pattern: "import static.*SearchEngine.LOG|static.*LOG"
---

<objective>
Replace all `System.Logger` calls with `SearchEngine.LOG` across all executor classes.

Purpose: Migrate executor logging to centralized, bundle-aware logger.
Output: All executor classes now use `SearchEngine.LOG` instead of `System.Logger`.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.02-fix-logging-use-plugin-logger/03-02-CONTEXT.md
@.gsd/phases/03.02-fix-logging-use-plugin-logger/03.02-01-SUMMARY.md

# Phase Context: API Migration Only
- Replace logger instance, keep message content identical
- No behavior changes, no new logging added
- Maintain existing log levels (debug, trace, warning, error)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace System.Logger with SearchEngine.LOG in executor classes</name>
  <files>
    src/org/modelio/platform/query/api/model/execution/QueryEngine.java
    src/org/modelio/platform/query/api/model/execution/SourceEvaluator.java
    src/org/modelio/platform/query/api/model/execution/ProvidedSourceCompute.java
    src/org/modelio/platform/query/api/model/execution/AllSourceCompute.java
  </files>
  <action>
    Systematically replace logger instances in each executor class.
    
    For each executor class identified in 03.02-01-SUMMARY.md:
    
    1. Find the current logger declaration:
       - Usually: `private static final System.Logger logger = System.getLogger(...)`
       - Or: Local logger instance in methods
    
    2. Replace with static import:
       - Add: `import static org.modelio.platform.search.engine.plugin.SearchEngine.LOG;`
       - Remove: Old System.Logger import
       - Remove: Local logger variable/field
    
    3. Replace all logger.debug/trace/warn/error calls:
       - FROM: `logger.log(Level.DEBUG, "message", args)`
       - TO: `LOG.debug("message", args)` or equivalent PluginLogger API
       - Keep message content identical, only change logger reference
    
    4. For string formatting, match PluginLogger API:
       - If PluginLogger uses System.Logger: calls stay similar
       - If PluginLogger uses different API: adapt call syntax only, not message
    
    5. Verify imports are clean (no unused System.Logger imports remain)
    
    6. Run formatter to maintain code style consistency
  </action>
  <verify>
    For each modified file:
    - grep confirms no "System.Logger" or "System.getLogger" remains
    - grep confirms SearchEngine.LOG is imported and used
    - All log() calls compile without error
    - Message content is identical to original
  </verify>
  <done>All executor classes migrated to SearchEngine.LOG, System.Logger references removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update any test classes or mock loggers to use SearchEngine.LOG</name>
  <files>
    src/org/modelio/platform/search/engine.test/**/*Test.java
    src/org/modelio/platform/query/api/model/execution/**/*Test.java
  </files>
  <action>
    If any test classes set up or verify logging:
    
    1. Search test files for System.Logger references:
       grep -r "System\.Logger" .../test/
    
    2. If found in mock/stub loggers or test setup:
       - Replace with SearchEngine.LOG where testing logging behavior
       - Or remove if just logging verification scaffolding
    
    3. Verify any logging assertions still work with new logger
    
    4. If no test references found, skip this task
  </action>
  <verify>
    grep confirms no System.Logger in test files related to executor.
    All logging tests still compile and run.
  </verify>
  <done>Test classes updated if needed, no System.Logger references in logger setup.</done>
</task>

<task type="auto">
  <name>Task 3: Verify SearchEngine.LOG is initialized before executor code runs</name>
  <files>src/org/modelio/platform/search/engine/plugin/SearchEngine.java</files>
  <action>
    Ensure no initialization-order problems:
    
    1. Locate SearchEngine.LOG static initializer:
       - Verify it runs before any executor classes are loaded
       - Check if it depends on bundle activation or other initializers
    
    2. If needed, add a static initialization guard in QueryEngine:
       - Static block that verifies SearchEngine.LOG is ready
       - Or lazy initialization pattern if SearchEngine.LOG might be null
    
    3. Add javadoc to SearchEngine.LOG noting executor dependency:
       Add comment: "Used by executor classes — must be initialized before QueryEngine.execute()"
    
    4. Verify no circular dependencies:
       - SearchEngine imports nothing from executor
       - Executor only imports from SearchEngine static field
  </action>
  <verify>
    SearchEngine.LOG initializes before executor usage.
    No circular dependencies detected.
    No NullPointerException risk from uninitialized logger.
  </verify>
  <done>Logger initialization order verified, no blocking issues for executor runtime.</done>
</task>

<task type="auto">
  <name>Task 4: Build and test to confirm replacement is complete and working</name>
  <files>N/A (build/test)</files>
  <action>
    Compile and verify no issues:
    
    1. Build the bundle:
       cd .../platform.search.engine
       mvn clean compile -q
    
    2. Run executor tests:
       mvn test -k "execution\|execute" -q 2>&1 | grep -E "FAIL|ERROR|OK"
    
    3. Grep confirms no System.Logger in compiled classes:
       grep -r "System\.Logger" target/classes/ 2>/dev/null | wc -l
       (Should be 0 or only in comments/strings)
    
    4. If grep finds any, trace and fix remaining references
  </action>
  <verify>
    Build completes without errors.
    All executor tests pass.
    grep confirms no System.Logger in compiled output.
    Logging works correctly at runtime (if observable in tests).
  </verify>
  <done>Replacement complete and verified through build/test cycle.</done>
</task>

</tasks>

<verification>
After completion:
1. All System.Logger imports and calls have been replaced with SearchEngine.LOG
2. Log message content is identical (only API changed)
3. Build succeeds without errors
4. Tests pass
5. No System.Logger references remain in executor path
</verification>

<success_criteria>
✓ Zero System.Logger references in executor code
✓ SearchEngine.LOG is imported and used consistently
✓ Build passes without errors
✓ All logging messages preserved (no behavior change)
✓ Tests pass and verify logging still works
</success_criteria>

<output>
After completion, create `.gsd/phases/03.02-fix-logging-use-plugin-logger/03.02-02-SUMMARY.md`
</output>
