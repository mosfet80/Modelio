---
phase: 09.1-code-review-driven-refactor
verified: 2026-05-19T00:00:00Z
status: passed
score: 14/14 must-haves verified
---

# Phase 09.1: Code-Review-Driven Refactor — Verification Report

**Phase Goal:** Refactor StepPipelineExecutor based on code review findings — restore pure streaming semantics, fix silent path drop, remove batching infrastructure, and add merge logic to SyncQueryResponse.
**Verified:** 2026-05-19
**Status:** ✅ PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Pipeline emits one `NodeWithExplainOut` per (node, path) pair — no batching | ✓ VERIFIED | `StepPipelineExecutor.java:153` — `downstream.pushItem(new NodeWithExplainOut(item.node(), List.of(item.path())))` |
| 2  | All source paths are iterated — no silent drop | ✓ VERIFIED | `StepPipelineExecutor.java:164` — `for (PathOutput path : item.whyPaths()) { chainHead.accept(new TraversalItem(item.node(), path)); }` iterates every path |
| 3  | Budget counts unique nodes via UUID-based dedup; subsequent paths for same node are free | ✓ VERIFIED | `StepPipelineExecutor.java:142-143` — `boolean isNewNode = emittedNodeUuids.add(uuid); if (isNewNode && !budget.tryAccept())` — `tryAccept()` only on first path |
| 4  | Truncation fires exactly once when budget exhausted | ✓ VERIFIED | `StepPipelineExecutor.java:126,145-146` — `boolean[] truncationFired = { false };` + guard `if (!truncationFired[0]) { truncationFired[0] = true; downstream.setTruncated(); …` |
| 5  | `NodeAccumulator` inner class deleted | ✓ VERIFIED | `grep -r NodeAccumulator src/` returns zero results — class absent from source tree |
| 6  | `SyncQueryResponse` groups multiple (node, path) emissions into merged entries | ✓ VERIFIED | `SyncQueryResponse.java:83-87` — `itemsByNode.merge(item.node(), item, (existing, incoming) -> merged paths set)` |
| 7  | `SyncQueryResponse.items()` returns one `NodeWithExplainOut` per unique node with all paths | ✓ VERIFIED | `SyncQueryResponse.java:70-71` — `List.copyOf(this.itemsByNode.values())` backed by `LinkedHashMap<MObject, NodeWithExplainOut>` |
| 8  | `PreparedAsyncExecution` fires `onItem` handlers for every (node, path) emission | ✓ VERIFIED | `PreparedAsyncExecution.java:39` class Javadoc — "The same node may trigger multiple handler invocations with different paths." `pushItem` → `notifyItemHandlers(item)` on every call |
| 9  | `IASyncExecution.onItem()` Javadoc documents multi-fire-per-node contract | ✓ VERIFIED | `IASyncExecution.java:40-42` — "The handler may be called multiple times for the **same node** with different explanation paths. Each invocation carries a `NodeWithExplainOut` with exactly one path." |
| 10 | `StepPipelineExecutor.java` contains `emittedNodeUuids` | ✓ VERIFIED | Lines 131, 142, 144 |
| 11 | Pattern `downstream.pushItem(new NodeWithExplainOut` present in `StepPipelineExecutor` | ✓ VERIFIED | Line 153 |
| 12 | `SyncQueryResponse.java` contains `itemsByNode` | ✓ VERIFIED | Lines 44, 71, 83 |
| 13 | `IASyncExecution.java` documents same-node multi-fire (or equivalent) | ✓ VERIFIED | Line 40 — "same node" with "multiple times" in `onItem` Javadoc |
| 14 | All existing tests pass (≥124 passed, 0 failures) | ✓ VERIFIED | `Tests run: 124, Failures: 0, Errors: 0, Skipped: 2` — exit code 0 |

**Score:** 14/14 must-haves verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `StepPipelineExecutor.java` | Pure streaming, one emit per (node, path), `emittedNodeUuids` budget, no `NodeAccumulator` | ✓ EXISTS · SUBSTANTIVE · CORRECT | 307 lines; `emittedNodeUuids`, `truncationFired`, `downstream.pushItem(new NodeWithExplainOut(..., List.of(item.path())))` present; no `NodeAccumulator` |
| `SyncQueryResponse.java` | `LinkedHashMap<MObject, NodeWithExplainOut> itemsByNode`, merge on `pushItem`, `items()` returns values | ✓ EXISTS · SUBSTANTIVE · CORRECT | 113 lines; `itemsByNode`, `merge()` with path union, `items()` returns `List.copyOf(this.itemsByNode.values())` |
| `IASyncExecution.java` | `onItem()` Javadoc documents multi-fire-per-node contract | ✓ EXISTS · SUBSTANTIVE · CORRECT | 90 lines; clear Javadoc on `onItem()` |
| `PreparedAsyncExecution.java` | `pushItem` fires all registered `onItem` handlers immediately | ✓ EXISTS · SUBSTANTIVE · CORRECT | 251 lines; `pushItem` → `notifyItemHandlers(item)` wired correctly |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `StepPipelineExecutor` terminal | `downstream.pushItem` | `new NodeWithExplainOut(item.node(), List.of(item.path()))` | ✓ WIRED | Line 153 — one emit per (node, path) pair |
| `StepPipelineExecutor` `pushItem` | `chainHead` | `for (PathOutput path : item.whyPaths())` | ✓ WIRED | Line 164 — all paths iterated, none dropped |
| `SyncQueryResponse.pushItem` | `itemsByNode` | `itemsByNode.merge(...)` with path union | ✓ WIRED | Lines 83-87 — merge accumulates paths per node |
| `SyncQueryResponse.items()` | `itemsByNode.values()` | `List.copyOf(this.itemsByNode.values())` | ✓ WIRED | Line 71 — returns deduplicated, path-merged entries |
| `PreparedAsyncExecution.responseFiller.pushItem` | `notifyItemHandlers` | direct call | ✓ WIRED | Inner filler forwards each push to all registered handlers |

---

## Success Criteria Check

| Criterion | Status | Evidence |
|-----------|--------|----------|
| All critical correctness issues resolved | ✓ | Pure streaming, budget via `emittedNodeUuids`, single-fire truncation |
| Pipeline emits all paths (none silently dropped) | ✓ | `for (PathOutput path : item.whyPaths())` iterates all paths |
| Dedup logic is pure (no side effects before emission) | ✓ | `emittedNodeUuids.add(uuid)` + rollback on budget failure; emit happens after dedup check |
| Streaming contract restored: `PreparedAsyncExecution` receives one event per (node, path) | ✓ | `pushItem(new NodeWithExplainOut(item.node(), List.of(item.path())))` confirmed |
| `SyncQueryResponse` correctly deduplicates into `Map<MObject, NodeWithExplainOut>` | ✓ | `LinkedHashMap<MObject, NodeWithExplainOut> itemsByNode` with merge |
| Javadoc accurate and complete | ✓ | Class and `onItem()` Javadocs updated in `StepPipelineExecutor`, `PreparedAsyncExecution`, `IASyncExecution` |
| All existing tests pass | ✓ | 124 passed, 0 failures, 2 skipped (pre-existing `@Disabled`) |

---

## Anti-Patterns Found

None. No TODOs, stubs, placeholders, or empty implementations found in the modified files.

---

## Human Verification Required

None. All success criteria are verifiable from source code structure and test results.

---

_Verified: 2026-05-19_
_Verifier: Copilot (gsd-verifier)_
