---
phase: 02.1-ensure-in-queryresolver-that-every-step-and-expr-interfaces-sub-classes-are-handled
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Coverage tests discover Step/Expr concrete subtypes at runtime and compare them against explicit test factories."
    - "Adding a new Step/Expr subtype without updating coverage tests fails with a clear assertion."
    - "Resolver coverage tests still ensure no unsupported subtype diagnostics for known steps/expressions."
  artifacts:
    - "platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java"
  key_links:
    - "Subtype discovery scans org.modelio.platform.query.api.model.query and feeds the factory registry assertions."
    - "Factory registries drive resolver coverage tests for Step and Expr subtypes."
---

<objective>
Make resolver coverage tests discover Step/Expr subtypes dynamically so new subtypes cannot slip through.

Purpose: Close the verification gap by enforcing subtype coverage for future additions.
Output: A runtime subtype discovery check wired into the coverage test suite.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

Relevant source files:
@platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java
@platform.search.engine/src/org/modelio/platform/query/api/model/query/Step.java
@platform.search.engine/src/org/modelio/platform/query/api/model/query/Expr.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subtype discovery and coverage assertions</name>
  <files>
    platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java
  </files>
  <action>
    Update the coverage test to discover concrete Step/Expr implementations at runtime and assert the factory registry is complete:

    - Add a helper like `discoverConcreteSubtypes(Class<?> baseType, String packageName)` that scans the classpath for classes under `org.modelio.platform.query.api.model.query`.
    - Support both `file:` and `jar:` URLs from `ClassLoader.getResources(packagePath)` so tests work in Tycho.
    - Filter out interfaces/abstract classes and only keep classes where `baseType.isAssignableFrom(clazz)` is true.
    - Introduce explicit factory maps for instantiation (e.g., `Map<Class<? extends Step>, Supplier<Step>>` and `Map<Class<? extends Expr>, Supplier<Expr>>`).
    - Assert that the discovered subtype set equals the factory map key set. Include an assertion message that lists missing or extra types so new subtypes fail loudly.
    - Replace the hard-coded lists in the tests with `factory.values()` to build steps/expressions, keeping the existing diagnostic assertions intact.

    This keeps explicit per-subtype factories while forcing updates when new subtypes are added.
  </action>
  <verify>
    QueryResolverCoverageTest compiles and fails if a new Step/Expr subtype is introduced without a factory entry.
  </verify>
  <done>
    Coverage test enumerates Step/Expr subtypes dynamically and asserts full factory coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run resolver-focused tests</name>
  <files>
    platform.search.engine.test/build/pom.xml
  </files>
  <action>
    Run the resolver test bundle with a focused pattern:

    `cd platform.search.engine.test/build && rm -f mvn-build.log mvn-exit.txt && (source /work/modelio/alouette/toolkit/env_toolkit.sh && mvn -q -DskipTests=false -Dtest='*QueryResolver*' verify |& tee mvn-build.log; echo $? > mvn-exit.txt)`

    Confirm success:
    `cat platform.search.engine.test/build/mvn-exit.txt`
  </action>
  <verify>
    Exit code is 0 and the coverage tests pass.
  </verify>
  <done>
    Resolver coverage tests pass with dynamic subtype discovery.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dynamic subtype discovery asserts Step/Expr factory coverage
- [ ] Coverage tests still detect unsupported subtype diagnostics for known cases
- [ ] Resolver-focused test run succeeds
</verification>

<success_criteria>

- Coverage tests enumerate Step/Expr subtypes dynamically and enforce factory updates.
- Adding a new Step/Expr subtype without updating coverage tests fails fast.
- Resolver-focused tests pass.
</success_criteria>

<output>
After completion, create `.gsd/phases/02.1-ensure-in-queryresolver-that-every-step-and-expr-interfaces-sub-classes-are-handled/02.1-02-SUMMARY.md`
</output>
