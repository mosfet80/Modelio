---
phase: 02.1-ensure-in-queryresolver-that-every-step-and-expr-interfaces-sub-classes-are-handled
plan: "01"
type: execute
wave: 1
depends_on:
  - "02-04"
files_modified:
  - platform.search.engine/src/org/modelio/platform/query/engine/resolver/QueryResolver.java
  - platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedStep.java
  - platform.search.engine/src/org/modelio/platform/query/engine/resolved/expr/ResolvedExpr.java
  - platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java
autonomous: true
user_setup: []
must_haves:
  truths:
    - "ResolvedStep/ResolvedExpr cover each current Step/Expr subtype (or explicit placeholders) so resolution is structurally complete."
    - "QueryResolver explicitly IMPLEMENTS every current Step subtype or throw an UnsupportedOperationException for really unknown cases."
    - "QueryResolver explicitly IMPLEMENTS every Expr subtype, returning resolved forms or throw an UnsupportedOperationException for really unknown cases." 
    - "Resolver tests lock in Step/Expr coverage so new subtypes require a resolver update."
  artifacts:
    - "platform.search.engine/src/org/modelio/platform/query/engine/resolver/QueryResolver.java"
    - "platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedStep.java"
    - "platform.search.engine/src/org/modelio/platform/query/engine/resolved/expr/ResolvedExpr.java"
    - "platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java"
  key_links:
    - "QueryResolver resolveStep/resolveExpr map all known query model subtypes to resolved counterparts."
    - "Coverage tests enumerate Step/Expr subclasses and assert resolver behavior per subtype."
---

<objective>
Guarantee QueryResolver handles and implements every Step and Expr subtype defined by the query model.

Purpose: Avoid silent gaps when new query model subtypes are added or existing ones are missed.
Output: Resolver updates plus tests that enforce Step/Expr coverage.
</objective>

<execution_context>
@.gsd/templates/phase-prompt.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md

Relevant source files:
@src/org/modelio/platform/query/engine/resolver/QueryResolver.java
@src/org/modelio/platform/query/engine/resolved/ResolvedStep.java
@src/org/modelio/platform/query/engine/resolved/expr/ResolvedExpr.java
@src/org/modelio/platform/query/api/model/query/Step.java
@src/org/modelio/platform/query/api/model/query/Expr.java
@src/org/modelio/platform/query/api/model/query/FilterStep.java
@src/org/modelio/platform/query/api/model/query/TraverseStep.java
@src/org/modelio/platform/query/api/model/query/UnionStep.java
@src/org/modelio/platform/query/api/model/query/RepeatStep.java
@src/org/modelio/platform/query/api/model/query/AndExpr.java
@src/org/modelio/platform/query/api/model/query/OrExpr.java
@src/org/modelio/platform/query/api/model/query/NotExpr.java
@src/org/modelio/platform/query/api/model/query/CompareExpr.java
@src/org/modelio/platform/query/api/model/query/IsExpr.java
@src/org/modelio/platform/query/api/model/query/ExistsExpr.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align resolved Step/Expr types with query subtypes</name>
  <files>
    platform.search.engine/src/org/modelio/platform/query/engine/resolved/ResolvedStep.java
    platform.search.engine/src/org/modelio/platform/query/engine/resolved/expr/ResolvedExpr.java
  </files>
  <action>
    Review the resolved model and ensure it matches the current Step/Expr subtype set:

    - ResolvedStep: confirm a resolved type exists for each Step subtype (Filter/Traverse/Union/Repeat), add explicit implementation if any are missing.
    - ResolvedExpr: confirm resolved types exist for each Expr subtype (And/Or/Not/Compare/Is/Exists) with full implementation.

    The goal is explicit, intentional coverage for each subtype, with full implementation, NO PLACEHOLDER.
  </action>
  <verify>
    ResolvedStep/ResolvedExpr compile with explicit subtype coverage and implementation.
  </verify>
  <done>
    Resolved model contains explicit coverage and implementation for each current Step/Expr subtype.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make Step and Expr handling explicit in QueryResolver</name>
  <files>
    platform.search.engine/src/org/modelio/platform/query/engine/resolver/QueryResolver.java
  </files>
  <action>
    Review resolveStep/resolveExpr and ensure every current Step/Expr subtype is explicitly handled:

    - Step: Filter, Traverse, Union, Repeat should each be in a dedicated branch.
    - Expr: And, Or, Not, Compare, Is, Exists should each be handled in the switch.
    - For known subtypes, keep or implement the intended behavior (e.g., resolve to ResolvedStep/ResolvedExpr) and ensure no generic "Unsupported step type" diagnostics are emitted for these cases.  
    - For any genuinely unknown cases (e.g., a new subtype added without resolver update), throw an UnsupportedOperationException with a clear message. The goal is to avoid silent "Unsupported step type" diagnostics for known subtypes and ensure new subtypes trigger explicit handling or clear exceptions.
    - Keep the existing behavior, but make diagnostics explicit and consistent for each handled subtype.
    - Preserve current output-kind logic and diagnostic wording for existing behaviors.

  </action>
  <verify>
    QueryResolver compiles with explicit coverage and implementation for all Step/Expr subtypes.
  </verify>
  <done>
    QueryResolver contains explicit handling for every Step/Expr subtype in the current API.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add resolver coverage tests for Step and Expr subtypes</name>
  <files>
    platform.search.engine.test/src/org/modelio/platform/query/engine/resolver/QueryResolverCoverageTest.java
  </files>
  <action>
    Add a focused test that instantiates each Step and Expr subtype and runs them through QueryResolver:

    - Build minimal instances for FilterStep, TraverseStep, UnionStep, RepeatStep, and each Expr subtype.
    - Use existing resolver tests as a fixture template (e.g., QueryResolverKindSmokeTest) to create a resolver context.
    - Assert each subtype is processed without falling into the generic "Unsupported step type" diagnostic.

    This test should fail when a new subtype is added without resolver handling.
  </action>
  <verify>
    Tests compile and run with the new coverage assertions.
  </verify>
  <done>
    Coverage tests enforce explicit Step/Expr handling in QueryResolver.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run resolver-focused tests</name>
  <files>
    platform.search.engine.test/build/pom.xml
  </files>
  <action>
    Run the resolver test bundle with a focused pattern:

    `cd platform.search.engine.test/build && rm -f mvn-build.log mvn-exit.txt && (source /work/modelio/alouette/toolkit/env_toolkit.sh && mvn -q -DskipTests=false -Dtest='*QueryResolver*' verify |& tee mvn-build.log; echo $? > mvn-exit.txt)`

    Confirm success:
    `cat platform.search.engine.test/build/mvn-exit.txt`
  </action>
  <verify>
    Exit code is 0 and the coverage test passes.
  </verify>
  <done>
    Resolver coverage tests pass.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] QueryResolver handles each Step/Expr subtype explicitly
- [ ] Coverage test passes without generic "Unsupported step type" diagnostics for known steps
- [ ] Resolver-focused test run succeeds
</verification>

<success_criteria>

- QueryResolver explicitly handles all Step/Expr subtypes in the current API.
- Coverage tests prevent missing resolver handling for new subtypes.
- Resolver-focused tests pass.
</success_criteria>

<output>
After completion, create `.gsd/phases/02.1-ensure-in-queryresolver-that-every-step-and-expr-interfaces-sub-classes-are-handled/02.1-01-SUMMARY.md`
</output>
