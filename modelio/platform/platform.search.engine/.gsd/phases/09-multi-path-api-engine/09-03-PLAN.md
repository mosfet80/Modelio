---
phase: 09-multi-path-api-engine
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - ../platform.search.engine.test/src/org/modelio/platform/query/api/model/result/PathOutputTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/TraverseStepExecutionTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/UnionStepExecutionTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/SourceEvaluatorTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/RepeatStepExecutionTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java
autonomous: true

must_haves:
  truths:
    - "PathOutputTest exists with tests for UUID-based equality and hashCode consistency"
    - "PathOutputTest verifies: same UUIDs different MObject refs → equal and same hashCode"
    - "PathOutputTest verifies: different UUIDs → not equal"
    - "All test .why() calls migrated to .whyPaths().iterator().next() or equivalent"
    - "All test new NodeWithExplainOut(node, path) calls migrated to new NodeWithExplainOut(node, Set.of(path))"
    - "Test fakes implementing IQueryResponseFiller have complete() method"
    - "All tests compile and pass (build/run-tests.sh exits 0)"
  artifacts:
    - path: "../platform.search.engine.test/src/org/modelio/platform/query/api/model/result/PathOutputTest.java"
      provides: "PathOutput equality contract tests"
      contains: "class PathOutputTest"
  key_links:
    - from: "../platform.search.engine.test/src/org/modelio/platform/query/api/model/result/PathOutputTest.java"
      to: "src/org/modelio/platform/search/engine/searchers/query/api/model/result/PathOutput.java"
      via: "tests UUID-based equals/hashCode override"
      pattern: "assertEquals"
---

<objective>
Migrate all test code to the new multi-path API and create PathOutput equality contract tests.

Purpose: After Plans 09-01 and 09-02 change the production API, tests won't compile. This plan
fixes all test compilation errors and adds PathOutputTest per CONTEXT.md Area 2 requirements.
Output: All tests compile and pass. PathOutputTest validates UUID-based equality semantics.
</objective>

<execution_context>
../.github/skills/execute-plan/SKILL.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/09-multi-path-api-engine/09-CONTEXT.md

Test source files (in platform.search.engine.test workspace folder):
@../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/TraverseStepExecutionTest.java
@../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/UnionStepExecutionTest.java
@../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/SourceEvaluatorTest.java
@../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/RepeatStepExecutionTest.java
@../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java

Production API (already changed by 09-01 and 09-02):
@src/org/modelio/platform/search/engine/searchers/query/api/model/result/NodeWithExplainOut.java
@src/org/modelio/platform/search/engine/searchers/query/api/model/result/PathOutput.java
@src/org/modelio/platform/search/engine/searchers/query/internal/executor/IQueryResponseFiller.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PathOutputTest class</name>
  <files>
    ../platform.search.engine.test/src/org/modelio/platform/query/api/model/result/PathOutputTest.java
  </files>
  <action>
Create a new test class `PathOutputTest` in the test bundle under package
`org.modelio.platform.query.api.model.result`.

Test scenarios (from CONTEXT.md Area 2):

1. **sameUuidsDifferentRefs_areEqual**: Create two MObject mocks/fakes returning the same UUID
   from `getUuid()` but as different object instances. Create two PathOutput instances with these
   nodes (same path structure). Assert `equals()` returns true and `hashCode()` values are the same.

2. **differentUuids_areNotEqual**: Create two MObject mocks/fakes with different UUIDs.
   Create two PathOutput instances. Assert `equals()` returns false.

3. **sameNodesDifferentSteps_areNotEqual**: Same node UUIDs but different PathStepOutput lists.
   Assert not equal.

4. **equalsNull_returnsFalse**: `pathOutput.equals(null)` returns false.

5. **reflexive**: `p.equals(p)` returns true.

**Mock pattern:** Check how existing tests create MObject instances. Look at:
- `PreparedAsyncExecutionTest` for `mockMObject()` patterns
- `../platform.search.engine.test/src/org/modelio/platform/query/testutils/` for test utilities

If the test bundle uses Mockito, use `Mockito.mock(MObject.class)` with `when(mock.getUuid()).thenReturn(uuid)`.
If no Mockito, check for hand-rolled fakes and follow that pattern.

Use JUnit 5 (`@Test`) and AssertJ assertions (`assertThat(...).isEqualTo(...)`).
  </action>
  <verify>
    File exists with at least 4 test methods covering the contract.
  </verify>
  <done>
    PathOutputTest validates UUID-based equality contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all test code to new multi-path API</name>
  <files>
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/TraverseStepExecutionTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/UnionStepExecutionTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/SourceEvaluatorTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/RepeatStepExecutionTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java
  </files>
  <action>
**2a. Fix IQueryResponseFiller.complete() in test fakes:**

Search all test files for classes/anonymous instances implementing `IQueryResponseFiller`:
```bash
grep -rn "IQueryResponseFiller\|implements.*Filler" ../platform.search.engine.test/src/
```

Add `@Override public void complete() {}` to each implementor found. Common locations:
- Anonymous `new IQueryResponseFiller() { ... }` in test code
- Helper classes in `SourceEvaluatorTest.java` (e.g., collecting filler)
- Look for `NoopDiagnosticSinkContractTest.java` — it tests the NOOP sink

**2b. Migrate .why() calls to .whyPaths():**

Find all `.why()` calls:
```bash
grep -rn "\.why()" ../platform.search.engine.test/src/
```

Known sites from research:
- `TraverseStepExecutionTest.java` — `classItem.get().why()` → `classItem.get().whyPaths().iterator().next()`
- `TraverseStepExecutionTest.java` — `animalItem.get().why()` → `animalItem.get().whyPaths().iterator().next()`
- `UnionStepExecutionTest.java` — `item.why()` → `item.whyPaths().iterator().next()`
- `SourceEvaluatorTest.java` — `item.why().nodes()` → `item.whyPaths().iterator().next().nodes()`
- Check `RepeatStepExecutionTest.java` for any `.why()` calls too

Each `.why()` becomes `.whyPaths().iterator().next()` (single-path scenarios still produce
single-element collections after accumulation).

**2c. Fix NodeWithExplainOut constructor calls in tests:**

```bash
grep -rn "new NodeWithExplainOut" ../platform.search.engine.test/src/
```

Change `new NodeWithExplainOut(node, path)` → `new NodeWithExplainOut(node, Set.of(path))`
Add `import java.util.Set;` where needed.

**2d. Fix any executeToList() / TraversalItem usage in tests:**

Since `TraversalItem` is package-private in `org.modelio.platform.search.engine.searchers.query.internal.executor`,
tests in package `org.modelio.platform.query.engine.executor` CANNOT access it directly. If any
test previously called `executeToList()` and worked with its return type, it needs adaptation.
Check if tests access `StepPipelineExecutor.executeToList()` — if so, they'd need to use
reflection or the method would need different access. More likely, tests go through `QueryEngine`
and see only `NodeWithExplainOut` results.

**2e. Update acceptance tests if needed:**

Check `../platform.search.engine.test/src/org/modelio/platform/query/engine/acceptance/` for any
`.why()` calls or `NodeWithExplainOut` constructor usage and fix those too.
  </action>
  <verify>
    Run full test suite:
    ```bash
    cd ../platform.search.engine.test && build/run-tests.sh
    ```
    Exit code must be 0.

    Verify no old API usage remains:
    ```bash
    grep -rn "\.why()" ../platform.search.engine.test/src/
    ```
    Should return no hits.
  </verify>
  <done>
    All tests compile and pass. No .why() calls remain. PathOutputTest validates equality.
    Pre-existing @Disabled tests (Q2, Q13) remain disabled — not Phase 9 scope.
  </done>
</task>

</tasks>

<verification>
```bash
cd ../platform.search.engine.test && build/run-tests.sh
```

Key checks:
```bash
# No .why() calls remain
grep -rn "\.why()" ../platform.search.engine.test/src/

# PathOutputTest exists
ls ../platform.search.engine.test/src/org/modelio/platform/query/api/model/result/PathOutputTest.java

# All IQueryResponseFiller test fakes have complete()
grep -rn "complete()" ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/*.java
```
</verification>
