---
phase: 05.1-refactor-step-pipeline-executor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/org/modelio/platform/query/internal/executor/TraverseStepExecutor.java
  - src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java
  - src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java
autonomous: true

must_haves:
  truths:
    - "StepPipelineExecutor is under 200 lines and contains only pipeline construction, chain wiring, step dispatch, filter, and union"
    - "TraverseStepExecutor contains all traverse and path-extension logic (~250 lines)"
    - "RepeatStepExecutor contains all BFS repeat logic (~120 lines)"
    - "All 98 existing tests pass with zero changes to test code"
    - "No @objid annotations exist in the two new files"
    - "The parameter 'mc' in extendPathForDep is renamed to 'sourceClass'"
  artifacts:
    - path: "src/org/modelio/platform/query/internal/executor/TraverseStepExecutor.java"
      provides: "Edge traversal (MDependency + LinkObject forward/backward), path extension, stereotype matching, element validation"
      contains: "wrapTraverse"
    - path: "src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java"
      provides: "BFS-level repeat traversal with cycle detection, maxDepth, emitAllDepths, globalVisited"
      contains: "wrapRepeat"
    - path: "src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java"
      provides: "Slimmed pipeline builder — buildPipeline, executeToList, buildChain, wrapStep, wrapFilter, wrapUnion"
      contains: "buildPipeline"
  key_links:
    - from: "StepPipelineExecutor.wrapStep"
      to: "TraverseStepExecutor.wrapTraverse"
      via: "case TraverseStep dispatch in switch expression"
      pattern: "TraverseStepExecutor\\.wrapTraverse"
    - from: "StepPipelineExecutor.wrapStep"
      to: "RepeatStepExecutor.wrapRepeat"
      via: "case RepeatStep dispatch in switch expression"
      pattern: "RepeatStepExecutor\\.wrapRepeat"
    - from: "RepeatStepExecutor.wrapRepeat"
      to: "StepPipelineExecutor.executeToList"
      via: "BFS body evaluation calls executeToList on body steps"
      pattern: "StepPipelineExecutor\\.executeToList"
---

<objective>
Extract `TraverseStepExecutor` and `RepeatStepExecutor` from `StepPipelineExecutor.java`,
applying minor cleanups (rename `mc` → `sourceClass`, add invariant comments, delete `@objid` from moved code).

Purpose: The 619-line `StepPipelineExecutor` has 5 responsibility clusters.
Extracting the two largest (traverse ~250 lines, repeat ~120 lines) leaves a focused
~180-line pipeline builder. This is pure structural refactoring with zero semantic changes.

Output: Three focused package-private classes in `org.modelio.platform.query.internal.executor`,
all 98 existing tests still passing.
</objective>

<execution_context>
.gsd/skills/execute-plan/SKILL.md
.gsd/templates/summary.md
</execution_context>

<context>
The source file to decompose is:
  src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java (619 lines)

The file contains these responsibility clusters:
  1. Pipeline construction: buildPipeline, executeToList, buildChain, wrapStep (~130 lines) — STAYS
  2. Filter/Union: wrapFilter, wrapUnion (~30 lines) — STAYS
  3. Repeat/BFS: wrapRepeat (~100 lines) — MOVES to RepeatStepExecutor
  4. Traverse: wrapTraverse, traverseMDependencyPush, traverseLinkObjectPush,
     traverseLinkForwardPush, traverseLinkBackwardPush, matchesStereotype, isValidElement (~200 lines) — MOVES to TraverseStepExecutor
  5. Path helpers: extendPathForDep, extendPathForLink (~25 lines) — MOVES to TraverseStepExecutor (only used by traverse)

All methods are private static or package-private static. Only buildPipeline is public.
The new classes each expose a single package-private static method (wrapTraverse, wrapRepeat)
called from StepPipelineExecutor.wrapStep.

RepeatStepExecutor.wrapRepeat needs access to StepPipelineExecutor.executeToList (already package-private).

Code style: explicit types (no var), this. for fields, Javadoc on class, delete @objid when moving code.

Build with: build/build.sh
Tests with: ../platform.search.engine.test/build/run-tests.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TraverseStepExecutor.java</name>
  <files>src/org/modelio/platform/query/internal/executor/TraverseStepExecutor.java</files>
  <action>
Create a new package-private final class `TraverseStepExecutor` in package
`org.modelio.platform.query.internal.executor`.

Move these methods from StepPipelineExecutor into TraverseStepExecutor:
- `wrapTraverse` (was private static → make package-private static)
- `traverseMDependencyPush` (keep private static)
- `traverseLinkObjectPush` (keep private static)
- `traverseLinkForwardPush` (keep private static)
- `traverseLinkBackwardPush` (keep private static)
- `matchesStereotype` (keep private static)
- `isValidElement` (keep private static)
- `extendPathForDep` (keep private static)
- `extendPathForLink` (keep private static)

When moving:
1. Copy the Modelio GPL copyright header from StepPipelineExecutor.
2. Add a class-level Javadoc: "Handles edge traversal for {@link ResolvedStep.TraverseStep} steps. Navigates MDependency and LinkObject edges, extends path outputs, and validates elements."
3. DELETE all @objid annotations from the moved methods and from the class declaration.
4. Rename the parameter `mc` to `sourceClass` in `extendPathForDep`.
5. Add own LOG field: `private static final PluginLogger LOG = SearchEngine.LOG;`
6. Add a private constructor to prevent instantiation.
7. Keep all method signatures, logic, and JavaDoc comments identical (except the mc→sourceClass rename).
8. Add the required imports (they can be copied from StepPipelineExecutor's import block — only include what's actually needed).
  </action>
  <verify>
The file compiles (check in next task after StepPipelineExecutor is updated). Verify:
- No @objid annotations present in the new file
- The parameter in extendPathForDep is named `sourceClass`, not `mc`
- Class is package-private (no `public` modifier)
- Only `wrapTraverse` is package-private static; all other methods are private static
  </verify>
  <done>TraverseStepExecutor.java exists with all 9 traverse/path/helper methods, no @objid, mc renamed to sourceClass.</done>
</task>

<task type="auto">
  <name>Task 2: Create RepeatStepExecutor.java</name>
  <files>src/org/modelio/platform/query/internal/executor/RepeatStepExecutor.java</files>
  <action>
Create a new package-private final class `RepeatStepExecutor` in package
`org.modelio.platform.query.internal.executor`.

Move from StepPipelineExecutor:
- `wrapRepeat` (was private static → make package-private static)

When moving:
1. Copy the Modelio GPL copyright header from StepPipelineExecutor.
2. Add a class-level Javadoc: "Handles BFS-level traversal for {@link ResolvedStep.RepeatStep} steps. Implements cycle detection, maxDepth enforcement, emitAllDepths support, and global visited tracking."
3. DELETE all @objid annotations.
4. Add own LOG field: `private static final PluginLogger LOG = SearchEngine.LOG;`
   (LOG is not currently used by wrapRepeat, but add it for consistency and future use.)
5. Add a private constructor to prevent instantiation.
6. The body evaluation call `executeToList(repeat.body(), List.of(frontierItem), session)` must become
   `StepPipelineExecutor.executeToList(repeat.body(), List.of(frontierItem), session)` since executeToList
   stays in StepPipelineExecutor (it's package-private already).
7. Keep all other logic, Javadoc, and comments identical.

Add two documentation comments addressing code review findings:
- Finding 1 (silent empty with globalVisited): Add a comment block before the `if (nextFrontier.isEmpty())` block:
  ```
  // NOTE (Finding 1): When emitAllDepths=false and depth==1, if nextFrontier is empty because
  // ALL body results were already in globalVisited, the depth > 1 guard prevents emission.
  // This is correct: the source node should not be emitted, and no new frontier exists.
  // A pipeline with two RepeatSteps sharing globalVisited can produce zero results for
  // the second step's source — this is accepted behaviour, not a bug.
  ```
- Finding 7 (depth > 1 invariant): Add a comment on the `depth > 1` guard line:
  ```
  // depth > 1 excludes source: at depth==1, frontier == [sourceItem] (the initial seed)
  ```
  </action>
  <verify>
The file compiles (check in next task after StepPipelineExecutor is updated). Verify:
- No @objid annotations present in the new file
- Class is package-private (no `public` modifier)
- Only `wrapRepeat` is package-private static
- The executeToList call is qualified: StepPipelineExecutor.executeToList(...)
- Finding 1 and Finding 7 comments are present
  </verify>
  <done>RepeatStepExecutor.java exists with wrapRepeat method, no @objid, invariant comments for findings 1 and 7, qualified executeToList call.</done>
</task>

<task type="auto">
  <name>Task 3: Slim down StepPipelineExecutor.java</name>
  <files>src/org/modelio/platform/query/internal/executor/StepPipelineExecutor.java</files>
  <action>
Update StepPipelineExecutor.java to remove all methods that were extracted:

1. DELETE the following methods (with their Javadoc and @objid annotations):
   - wrapRepeat (lines ~308-406)
   - wrapTraverse (lines ~418-440)
   - traverseMDependencyPush (lines ~451-473)
   - traverseLinkObjectPush (lines ~483-498)
   - traverseLinkForwardPush (lines ~510-555)
   - traverseLinkBackwardPush (lines ~565-605)
   - matchesStereotype (lines ~580-592)
   - isValidElement (lines ~598-605)
   - extendPathForDep (lines ~615-619)
   - extendPathForLink (lines ~609-613)
   - Also delete the "// Repeat step" and "// Traverse step" and "// Helpers" section divider comments

2. UPDATE the `wrapStep` switch expression to delegate to the new classes:
   ```java
   case TraverseStep t -> TraverseStepExecutor.wrapTraverse(t, downstream, session);
   case RepeatStep r -> RepeatStepExecutor.wrapRepeat(r, downstream, session, limits, globalVisited, diagnosticSink);
   ```
   (FilterStep, UnionStep, UnsupportedStep cases remain unchanged)

3. REMOVE unused imports that were only needed by the extracted methods. The following imports
   should no longer be needed in StepPipelineExecutor:
   - org.modelio.metamodel.uml.infrastructure.ModelElement
   - org.modelio.platform.query.api.model.query.Direction
   - org.modelio.platform.query.api.model.resolved.ResolvedEdge
   - org.modelio.platform.query.api.model.resolved.ResolvedEdge.EdgeAlternative
   - org.modelio.platform.query.api.model.resolved.ResolvedEdge.LinkObjectEdge
   - org.modelio.platform.query.api.model.resolved.ResolvedEdge.MDependencyEdge
   - org.modelio.platform.query.api.model.result.LinkPathStepOutput
   - org.modelio.platform.query.api.model.result.MDependencyPathStepOutput
   - org.modelio.platform.query.api.model.result.PathOutput
   - org.modelio.platform.query.api.model.result.PathStepOutput
   - org.modelio.vcore.smkernel.mapi.MClass
   - org.modelio.vcore.smkernel.mapi.MDependency
   - org.modelio.vcore.smkernel.mapi.MExpert
   - org.modelio.vcore.smkernel.mapi.MObject (KEEP — still used in buildPipeline via globalVisited Set&lt;MObject&gt;)

   Actually, check each import: MObject is still used (globalVisited), keep it. Remove only truly unreferenced ones.

4. KEEP these methods and fields unchanged: LOG, NOOP_STATE, NOOP_DIAGNOSTIC_SINK, constructor,
   buildPipeline, executeToList, buildChain, wrapStep (updated), wrapFilter, wrapUnion.

The resulting file should be approximately 180–200 lines.
  </action>
  <verify>
Run `build/build.sh` from the platform.search.engine directory. Expect: BUILD SUCCESS.
Verify StepPipelineExecutor.java is under 210 lines (approximately 180-200 expected).
  </verify>
  <done>StepPipelineExecutor.java is slimmed to ~180 lines, delegates traverse and repeat to new classes, compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Build and test verification</name>
  <files></files>
  <action>
Run the full build and test suite to confirm zero regressions:

1. Build the main module: run `build/build.sh` from the platform.search.engine directory.
   Expect: BUILD SUCCESS with exit code 0.

2. Run all tests: run `build/run-tests.sh` from the platform.search.engine.test directory.
   Expect: all 98 tests pass (Tests run: 98, Failures: 0, Errors: 0).

If any test fails, the refactoring introduced a regression — investigate and fix.
Common issues to watch for:
- Forgotten import in new classes
- Visibility mismatch (method needs to be package-private, not private)
- Missing static qualifier on method
- executeToList call not properly qualified in RepeatStepExecutor
  </action>
  <verify>
build/build.sh exits with code 0 (BUILD SUCCESS).
build/run-tests.sh shows 98 tests passing, 0 failures, 0 errors.
  </verify>
  <done>Build succeeds and all 98 existing tests pass unchanged — structural refactoring is regression-free.</done>
</task>

</tasks>

<verification>
1. StepPipelineExecutor.java is under 210 lines and contains only: buildPipeline, executeToList, buildChain, wrapStep, wrapFilter, wrapUnion, NOOP_DIAGNOSTIC_SINK, LOG.
2. TraverseStepExecutor.java exists with wrapTraverse (package-private static) + 8 private static helpers.
3. RepeatStepExecutor.java exists with wrapRepeat (package-private static) + invariant comments.
4. No @objid annotations exist in the two new files.
5. The `mc` parameter is renamed to `sourceClass` in extendPathForDep.
6. `build/build.sh` exits 0 and `build/run-tests.sh` shows 98 tests passing.
</verification>

<success_criteria>
- All 98 tests pass with zero test code changes
- StepPipelineExecutor under 210 lines
- TraverseStepExecutor and RepeatStepExecutor exist as package-private classes
- No @objid in extracted files
- Clean build with no warnings related to the refactoring
</success_criteria>

<output>
After completion, create `.gsd/phases/05.1-refactor-step-pipeline-executor/05.1-01-SUMMARY.md`
</output>
