---
phase: 03.03
plan: 02
type: execute
wave: 2
depends_on: ["03.03-01"]
files_modified:
  - src/org/modelio/platform/query/internal/QueryEngine.java
  - src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  - src/org/modelio/platform/query/internal/executor/PreparedAsyncExecution.java
autonomous: true
must_haves:
  truths:
    - "`prepareAsync(Query)` returns a not-started execution handle that does no work before `run()`."
    - "`run()` is single-use and completes with terminal `QueryResponseState` when execution ends or fails."
    - "`syncExec(Query)` blocks until completion and returns a fully terminal response."
    - "Late callback registration and repeated `run()` calls fail fast."
    - "`cancel()` transitions running async execution to cancellation semantics using existing diagnostics/state behavior."
  artifacts:
    - path: "src/org/modelio/platform/query/internal/QueryEngine.java"
      provides: "Implementation of new split engine API"
      contains: "syncExec"
    - path: "src/org/modelio/platform/query/internal/executor/PreparedAsyncExecution.java"
      provides: "Concrete async handle with callback registration, run/cancel lifecycle"
      contains: "onComplete"
    - path: "src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java"
      provides: "Bridge for callback emission and blocking sync execution consumption"
      contains: "state"
  key_links:
    - from: "QueryEngine.prepareAsync(Query)"
      to: "PreparedAsyncExecution"
      via: "new handle construction"
      pattern: "new PreparedAsyncExecution"
    - from: "PreparedAsyncExecution.run()"
      to: "StreamingQueryResponse"
      via: "response creation and callback pumping"
      pattern: "StreamingQueryResponse"
    - from: "QueryEngine.syncExec(Query)"
      to: "PreparedAsyncExecution.run()"
      via: "blocking wait for terminal state"
      pattern: "join|get"
---

<objective>
Implement the new execution lifecycle internally: prepared async handle + blocking sync path on top.

Purpose: make the public API usable end-to-end with explicit lifecycle semantics and fail-fast misuse checks.
Output: working `QueryEngine` implementation and concrete async execution handle wired to existing executor internals.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.03-refactor-again-response-api/03.03-CONTEXT.md
@.gsd/phases/03.03-refactor-again-response-api/03.03-01-SUMMARY.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement QueryEngine split methods using existing resolution/execution pipeline</name>
  <files>
    src/org/modelio/platform/query/internal/QueryEngine.java
  </files>
  <action>
    Refactor `QueryEngine` to implement new `IQueryEngine` surface:
    - Replace `execute(Query)` with `prepareAsync(Query)` + `syncExec(Query)`.
    - Keep existing query validation/hydration/resolution logic behavior unchanged (same diagnostics/state intent).
    - Ensure `prepareAsync(Query)` builds a not-started execution object.
    - Ensure `syncExec(Query)` performs blocking execution and returns only once execution is terminal.
    - Keep unresolved-framework failure behavior aligned with context (hybrid contract: diagnostics/state for query/runtime issues, throwable for unrecoverable framework failures).
  </action>
  <verify>
    `grep -n "prepareAsync\|syncExec\|execute(Query)" src/org/modelio/platform/query/internal/QueryEngine.java` confirms new methods and removal of old method.
    `grep -n "resolveQuery\|ProvidedSource\|selectionProvider" src/org/modelio/platform/query/internal/QueryEngine.java` confirms existing pipeline is still wired.
  </verify>
  <done>QueryEngine implements split execution methods while preserving current resolution/source-evaluation semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add PreparedAsyncExecution lifecycle implementation and callback wiring</name>
  <files>
    src/org/modelio/platform/query/internal/executor/PreparedAsyncExecution.java
    src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  </files>
  <action>
    Implement concrete async handle with strict lifecycle rules:
    - Create `PreparedAsyncExecution` implementing `IASyncExecution`.
    - Support multiple handlers for items/diagnostics/completion with fluent registration.
    - Reject any handler registration after run started (fail-fast exception).
    - Enforce `run()` single-use; second run fails fast.
    - `run()` starts execution and returns `CompletableFuture<QueryResponseState>` completed on terminal state.
    - Add/adjust minimal hooks needed in `StreamingQueryResponse` (or adapter logic) to feed callback streams safely and invoke completion handlers.
    - Implement `cancel()` by delegating to underlying running response lifecycle.
  </action>
  <verify>
    `grep -n "class PreparedAsyncExecution\|onItem\|onDiagnostic\|onComplete\|run\|cancel" src/org/modelio/platform/query/internal/executor/PreparedAsyncExecution.java` confirms lifecycle surface.
    `grep -n "IllegalStateException\|fail fast\|state" src/org/modelio/platform/query/internal/executor/PreparedAsyncExecution.java` confirms misuse guards.
  </verify>
  <done>Async execution lifecycle is implemented with pre-run registrations, explicit start, completion future, and cancellation support.</done>
</task>

<task type="auto">
  <name>Task 3: Validate build and targeted async lifecycle behavior</name>
  <files>
    src/org/modelio/platform/query/internal/QueryEngine.java
    src/org/modelio/platform/query/internal/executor/PreparedAsyncExecution.java
    src/org/modelio/platform/query/internal/executor/StreamingQueryResponse.java
  </files>
  <action>
    Validate implementation at module level before test migration:
    - Build search engine module to ensure API/implementation coherence.
    - Run a focused subset of executor tests that still compile after refactor.
    - Confirm no remaining internal references to removed `execute(Query)` method.
    Keep fixes in-scope for this phase only.
  </action>
  <verify>
    `bash ./build/build.sh` exits 0 in `platform.search.engine`.
    `grep -R "\.execute(query)" src/org/modelio/platform/query --line-number` returns no hits in module sources.
  </verify>
  <done>Implementation compiles cleanly and the codebase is internally migrated to split sync/async engine API.</done>
</task>

</tasks>

<verification>
- QueryEngine exposes working sync and prepared-async behavior.
- Async handle lifecycle constraints are enforced by implementation, not only documented.
- Module build is green after contract migration.
</verification>

<success_criteria>
- `prepareAsync(Query)` is lazy (no execution before `run()`) and returns configurable async handle.
- `run()` is single-use and returns terminal state future.
- `cancel()` and `state()` operate consistently with existing response lifecycle semantics.
- `syncExec(Query)` is blocking and returns only terminal responses.
</success_criteria>

<output>
After completion, create `.gsd/phases/03.03-refactor-again-response-api/03.03-02-SUMMARY.md`
</output>
