---
phase: 03.03-refactor-again-response-api
plan: 01
subsystem:
  api
tags: [query-engine, api-contract, sync-exec, async-execution, lifecycle]

requires:
  - phase: 03.01-simplify-asynciterator-api/03.01-03
    provides: QueryResponseState lifecycle contract used by async execution handle
provides:
  - Split public query engine execution API into blocking sync and prepared async entry points
  - New IASyncExecution contract with fluent callbacks and explicit run/cancel/state lifecycle
  - QueryEngine signature parity with IQueryEngine syncExec/prepareAsync contracts
affects: [phase-04-core-semantics, app-model-search-ui, query-engine-consumers]

tech-stack:
  added: []
  patterns:
    - explicit split between blocking execution and prepared asynchronous lifecycle
    - pre-run callback registration and single-use async start contract

key-files:
  created:
    - src/org/modelio/platform/query/api/IASyncExecution.java
  modified:
    - src/org/modelio/platform/query/api/IQueryEngine.java
    - src/org/modelio/platform/query/api/Queries.java
    - src/org/modelio/platform/query/internal/QueryEngine.java

key-decisions:
  - "Public execution API is intentionally breaking: remove execute(Query), keep only syncExec(Query) and prepareAsync(Query)."
  - "Asynchronous execution uses a prepared handle model with pre-run handler registration, explicit run(), and single-use fail-fast semantics."

patterns-established:
  - "IQueryEngine exposes intent-first execution methods by mode (sync vs async-prepared) instead of overloaded behavior."
  - "Async contracts expose lifecycle through QueryResponseState and explicit cancel/state controls."

duration: 8min
completed: 2026-02-20
---

# Phase 03.03 Plan 01: Public split execution API contract Summary

**Query engine public API now exposes explicit blocking sync execution and prepared async execution contracts, removing the legacy ambiguous execute(Query) entry point.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-02-20T10:06:30Z
- **Completed:** 2026-02-20T10:14:41Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Replaced `IQueryEngine.execute(Query)` with explicit `syncExec(Query)` and `prepareAsync(Query)` methods.
- Added `IASyncExecution` public contract with fluent callback registration (`onItem`, `onDiagnostic`, `onComplete`) and lifecycle controls (`run`, `cancel`, `state`).
- Updated `Queries` javadocs and aligned `QueryEngine` method signatures to the new API shape.

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace legacy engine contract with split sync/async methods** - `1fe5257` (feat)
2. **Task 2: Introduce async execution handle contract with lifecycle guards** - `65e905b` (feat)

## Files Created/Modified

- `src/org/modelio/platform/query/api/IQueryEngine.java` - split execution entry points (`syncExec`, `prepareAsync`) and updated API contract docs.
- `src/org/modelio/platform/query/api/Queries.java` - create-engine javadocs aligned to split execution model.
- `src/org/modelio/platform/query/api/IASyncExecution.java` - new prepared async execution lifecycle contract.
- `src/org/modelio/platform/query/internal/QueryEngine.java` - implementation signature parity with new engine contract.

## Decisions Made

- Keep the breaking API shape from phase context: no compatibility bridge for `execute(Query)`.
- Freeze async lifecycle semantics at contract level now (pre-run registration, explicit run, single-use fail-fast), before full async executor implementation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added QueryEngine signature parity to keep implementation aligned with the new interface**

- **Found during:** Task 2 (Introduce async execution handle contract with lifecycle guards)
- **Issue:** Updating `IQueryEngine` without corresponding `QueryEngine` method signatures would break interface implementation parity/compilation.
- **Fix:** Renamed implementation entry point to `syncExec(Query)` and added `prepareAsync(Query)` placeholder contract path.
- **Files modified:** `src/org/modelio/platform/query/internal/QueryEngine.java`
- **Verification:** Module build script completed with exit code 0.
- **Committed in:** `65e905b` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope creep; required to preserve compile-ready API contract parity.

## Issues Encountered

- Build script output included repeated p2 help text noise; build still completed successfully (`build/mvn-exit.txt` = `0`).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Public API shape for split sync/async execution is now frozen for downstream implementation work.
- Consumers must migrate from `execute(Query)` to `syncExec(Query)`/`prepareAsync(Query)` in subsequent plans.

---

_Phase: 03.03-refactor-again-response-api_
_Completed: 2026-02-20_
