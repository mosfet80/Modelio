---
phase: 03.03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/org/modelio/platform/query/api/IQueryEngine.java
  - src/org/modelio/platform/query/api/IASyncExecution.java
  - src/org/modelio/platform/query/api/Queries.java
autonomous: true
must_haves:
  truths:
    - "The query engine API exposes two explicit entry points: blocking `syncExec(Query)` and prepared async `prepareAsync(Query)`."
    - "Async execution handle supports pre-run handler registration and explicit start with `run()`."
    - "Legacy `execute(Query)` is removed from public API and cannot be used by consumers."
  artifacts:
    - path: "src/org/modelio/platform/query/api/IQueryEngine.java"
      provides: "Public engine contract with split sync/async methods"
      contains: "syncExec"
    - path: "src/org/modelio/platform/query/api/IASyncExecution.java"
      provides: "Prepared async execution contract with callbacks, run(), cancel(), state()"
      contains: "onItem"
    - path: "src/org/modelio/platform/query/api/Queries.java"
      provides: "Entry-point javadocs aligned with split execution model"
      contains: "createEngine"
  key_links:
    - from: "IQueryEngine.prepareAsync(Query)"
      to: "IASyncExecution"
      via: "return type contract"
      pattern: "prepareAsync"
    - from: "IQueryEngine"
      to: "QueryEngine"
      via: "implementation signature parity"
      pattern: "syncExec|prepareAsync"
---

<objective>
Define and freeze the new public execution API shape before implementation.

Purpose: establish the intentional breaking change and eliminate ambiguity between synchronous and asynchronous execution.
Output: compile-ready API contracts for `syncExec(Query)` and `prepareAsync(Query)` with explicit async lifecycle semantics.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.03-refactor-again-response-api/03.03-CONTEXT.md
@.gsd/phases/03.01-simplify-asynciterator-api/03.01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy engine contract with split sync/async methods</name>
  <files>
    src/org/modelio/platform/query/api/IQueryEngine.java
    src/org/modelio/platform/query/api/Queries.java
  </files>
  <action>
    Update public engine API to match locked phase decisions:
    - Remove `execute(Query)` from `IQueryEngine`.
    - Add `syncExec(Query)` as blocking execution entry point.
    - Add `prepareAsync(Query)` returning a not-started async handle.
    - Align javadocs in `IQueryEngine` and `Queries` to clearly describe blocking vs prepared-async behavior.
    Keep API changes focused to method-level contract updates only (no executor internals in this task).
  </action>
  <verify>
    `grep -n "syncExec\|prepareAsync\|execute(Query)" src/org/modelio/platform/query/api/IQueryEngine.java` shows `syncExec` and `prepareAsync`, and no `execute(Query)` declaration.
    `grep -n "createEngine\|syncExec\|prepareAsync" src/org/modelio/platform/query/api/Queries.java` confirms docs mention split API.
  </verify>
  <done>Public engine API contract is split into explicit sync and async entry points with no legacy execute method.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce async execution handle contract with lifecycle guards</name>
  <files>
    src/org/modelio/platform/query/api/IASyncExecution.java
  </files>
  <action>
    Add the new async execution handle interface in public API package:
    - Name: `IASyncExecution`.
    - Fluent pre-run registrations: `onItem(...)`, `onDiagnostic(...)`, `onComplete(...)` returning same handle type.
    - Lifecycle operations: `run()` returning `CompletableFuture<QueryResponseState>`, `cancel()`, and `state()`.
    - Contract notes: handlers must be registered before run, late registration fails fast, run is single-use and fails fast on repeated calls.
    Use explicit Java types (no `var`) and keep API surface minimal to this phase scope.
  </action>
  <verify>
    `grep -n "interface IASyncExecution\|onItem\|onDiagnostic\|onComplete\|run\|cancel\|state" src/org/modelio/platform/query/api/IASyncExecution.java` confirms required members.
  </verify>
  <done>`IASyncExecution` exists with fluent callback registration, explicit start/cancel operations, and state exposure.</done>
</task>

</tasks>

<verification>
- Public API compiles with new split execution surface.
- Legacy `execute(Query)` contract is no longer present in `IQueryEngine`.
- Async lifecycle constraints are documented in API contract (pre-run registration, single-use run, fail-fast misuse).
</verification>

<success_criteria>
- `IQueryEngine` exposes exactly `syncExec(Query)` and `prepareAsync(Query)` for execution entry.
- `IASyncExecution` defines callback contract, lifecycle methods, and state contract as decided in `03.03-CONTEXT.md`.
- No compatibility bridge/deprecation method for `execute(Query)` remains.
</success_criteria>

<output>
After completion, create `.gsd/phases/03.03-refactor-again-response-api/03.03-01-SUMMARY.md`
</output>
