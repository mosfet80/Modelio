---
phase: 03.03
plan: 03
type: execute
wave: 3
depends_on: ["03.03-02"]
files_modified:
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/QueryEngineExecutionTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/StreamingQueryResponseTest.java
  - ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java
  - src/org/modelio/platform/query/api/IQueryEngine.java
  - src/org/modelio/platform/query/internal/QueryEngine.java
autonomous: true
must_haves:
  truths:
    - "All engine tests use the new split API (`syncExec` / `prepareAsync`) and no longer call `execute(Query)`."
    - "Async callback ordering is validated per stream (items ordered within item callbacks, diagnostics ordered within diagnostic callbacks)."
    - "Lifecycle misuse scenarios are covered (late handler registration, repeated run, cancel before/after run)."
    - "Phase output demonstrates intentional breaking change completed without compatibility bridge."
  artifacts:
    - path: "../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/QueryEngineExecutionTest.java"
      provides: "Sync and async execution integration coverage"
      contains: "syncExec"
    - path: "../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java"
      provides: "Dedicated lifecycle and callback contract coverage"
      contains: "run"
    - path: "src/org/modelio/platform/query/api/IQueryEngine.java"
      provides: "No legacy execute() signature remains"
      contains: "prepareAsync"
  key_links:
    - from: "QueryEngineExecutionTest"
      to: "IQueryEngine.syncExec/prepareAsync"
      via: "updated API calls"
      pattern: "syncExec|prepareAsync"
    - from: "PreparedAsyncExecutionTest"
      to: "PreparedAsyncExecution"
      via: "callback/lifecycle assertions"
      pattern: "onItem|onDiagnostic|onComplete|run|cancel"
---

<objective>
Complete migration to the new API by updating tests and enforcing breaking-change cleanup.

Purpose: prove the redesigned response/execution API is actually consumable and behaviorally correct for both sync and async usage.
Output: updated tests, removal of legacy call sites, and passing targeted test/build verification.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/03.03-refactor-again-response-api/03.03-CONTEXT.md
@.gsd/phases/03.03-refactor-again-response-api/03.03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate existing query engine integration tests to split API</name>
  <files>
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/QueryEngineExecutionTest.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/StreamingQueryResponseTest.java
  </files>
  <action>
    Update current tests to use the new engine contract:
    - Replace `engine.execute(query)` with `engine.syncExec(query)` where blocking semantics are expected.
    - Add async path assertions using `prepareAsync(query)` + callback registration + `run()`.
    - Keep existing semantics checks (diagnostics/state/truncation) unless intentionally changed by phase decisions.
    - Ensure assertions reflect per-stream ordering guarantees, not cross-stream total ordering.
  </action>
  <verify>
    `grep -R "\.execute(query)" ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor --line-number` returns no hits.
    `grep -n "syncExec\|prepareAsync\|run()" ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/QueryEngineExecutionTest.java` confirms migrated API usage.
  </verify>
  <done>Integration tests target only the new split API and preserve intended runtime semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated async lifecycle contract tests</name>
  <files>
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java
  </files>
  <action>
    Add focused tests for async handle semantics:
    - Multiple handlers per stream are supported.
    - Late registration after start fails fast.
    - `run()` second invocation fails fast.
    - `onComplete` is invoked once with terminal state.
    - `cancel()` behavior before and after `run()` follows lifecycle contract.
    Use existing test utilities and avoid introducing new production semantics in tests.
  </action>
  <verify>
    `grep -n "late\|run\|cancel\|onComplete\|onItem\|onDiagnostic" ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/PreparedAsyncExecutionTest.java` confirms lifecycle coverage.
  </verify>
  <done>Async lifecycle guarantees are covered by dedicated regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run targeted and full module verification, then enforce cleanup gate</name>
  <files>
    src/org/modelio/platform/query/api/IQueryEngine.java
    src/org/modelio/platform/query/internal/QueryEngine.java
    ../platform.search.engine.test/src/org/modelio/platform/query/engine/executor/*.java
  </files>
  <action>
    Validate the phase completion and intentional breaking-change gate:
    - Run targeted executor tests including new async lifecycle test.
    - Run full search-engine test bundle.
    - Run search-engine module build.
    - Confirm no remaining `execute(Query)` API or call sites in platform.search.engine + platform.search.engine.test.
  </action>
  <verify>
    `bash ../platform.search.engine.test/build/run-tests.sh -Dtest=QueryEngineExecutionTest,StreamingQueryResponseTest,PreparedAsyncExecutionTest` exits 0.
    `bash ../platform.search.engine.test/build/run-tests.sh` exits 0.
    `bash ./build/build.sh` exits 0 in `platform.search.engine`.
    `grep -R "execute(Query" src ../platform.search.engine.test/src --line-number` returns no hits.
  </verify>
  <done>Refactor is validated by tests/build, and legacy execute API usage is fully removed.</done>
</task>

</tasks>

<verification>
- New split API is exercised in both sync and async test flows.
- Async lifecycle misuse protections are explicitly tested.
- Full module build/tests pass with no legacy execute API usage.
</verification>

<success_criteria>
- All call sites in module + tests use `syncExec` / `prepareAsync`.
- Async handle contract is proven by dedicated lifecycle tests.
- Build + test verification confirms breaking migration is complete and stable.
</success_criteria>

<output>
After completion, create `.gsd/phases/03.03-refactor-again-response-api/03.03-03-SUMMARY.md`
</output>
