---
phase: 03-executor-foundations
task: 1
total_tasks: 1
plan: 03-02
status: in_progress
last_updated: 2026-02-18
---

<current_state>
Phase 3 plan 03-01 is complete (committed as `a0b5ac1`). Plan 03-02 has NOT been started yet — no SUMMARY.md exists for it.

We are at the start of plan 03-02: implement `SourceEvaluator` + wire `QueryEngine.execute()` + `QueryEngineExecutionTest`.

The execute-phase run was paused before spawning the 03-02 subagent (the user requested a pause).
</current_state>

<completed_work>

- Phase 03-01: COMPLETE (committed `a0b5ac1`)
  - `DiagnosticCode` enum (16 codes)
  - `Diagnostic` record updated (code field added)
  - `Diagnostics` factory updated (all methods require DiagnosticCode)
  - All resolver callers updated (EdgeResolver, KindResolver, PropertyKeyResolver, QueryResolver)
  - `selectionProvider` broadened from `Supplier<MObject>` to `Supplier<Iterable<MObject>>`
  - `BufferedAsyncIterator<T>`, `EmptyAsyncIterator<T>`, `StreamingQueryResponse` created
  - `@JsonCreator fromJson()` added to `FrozenProvidedSource`
  - Tests: 42/42 green (`BufferedAsyncIteratorTest`, `StreamingQueryResponseTest`, `DiagnosticsSmokeTest`)
</completed_work>

<remaining_work>

- Plan 03-02: NOT STARTED
  - Task 1: Implement `SourceEvaluator.java` (handles `ProvidedSource` and `AllSource`, validity checks, diagnostics)
  - Task 1: Wire `QueryEngine.execute()` — resolve query, check resolution errors → refuse if errors, create `StreamingQueryResponse` with source evaluation task
  - Task 1: Create `QueryEngineExecutionTest.java` (5 integration tests: providedSource, emptyProvided, allSource, dispose, resolutionError)
  - Run build + all tests green
  - Create `03-02-SUMMARY.md`

Plan 03-02 is the final plan to complete Phase 3.
</remaining_work>

<decisions_made>

- `selectionProvider` returns `Iterable<MObject>` (not single element) — broadened in 03-01
- `ProvidedSource` in resolved model already holds elements from `selectionProvider` (check `QueryResolver` to confirm before writing `SourceEvaluator`)
- Null return from `selectionProvider.get()` must throw `NullPointerException` as a programming error (before `StreamingQueryResponse` is created)
- Unknown Step/Expr subtypes throw `UnsupportedOperationException` (fail loudly)
- Sole consumer path for `BufferedAsyncIterator`: `supplyAsync take()` only (eliminates race with `drainToPending()`)
- `DiagnosticCode` as enum (not String) for structured machine-readable codes
- Background daemon thread in `StreamingQueryResponse` for cancellation checking
- Resolution diagnostics pre-loaded before background thread starts
</decisions_made>

<blockers>
None. Phase 3-01 is fully committed and clean. The codebase is in a healthy state for 03-02.

Minor: `.github/prompts/execute-phase.prompt.md` has an uncommitted local modification (unrelated to feature work). `.gsd/tmp/` exists as untracked — ignore it.
</blockers>

<context>
Phase 3 goal is "Executor can execute a resolved query with a provided source and no steps, and results + diagnostics are delivered via AsyncIterator and can be disposed safely."

03-01 built the plumbing (streaming types, DiagnosticCode). 03-02 is the capstone: wire it all together so `QueryEngine.execute()` produces a real `StreamingQueryResponse` for trivial (no-step) queries. After 03-02, Phase 3 is done and can be verified.

The plan file at `.gsd/phases/03-executor-foundations/03-02-PLAN.md` has the full detailed spec — read it carefully before implementing. Key points:
1. `SourceEvaluator` is a new file in `platform.search.engine/src/.../internal/executor/`
2. The test class extends `AbstractMqlTest`
3. Check `QueryResolver` to confirm whether `ProvidedSource.elements()` is already populated at resolution time or needs to be fetched from `selectionProvider` in `QueryEngine.execute()`
4. Resolution error check: look for `ERROR` level diagnostics in resolved query — if any, return empty response with `RESOLUTION_ERROR` diagnostic, do NOT run SourceEvaluator
</context>

<next_action>
Start with: Read `03-02-PLAN.md` in full, then read `QueryResolver.java` to confirm how `ProvidedSource` is resolved (especially whether `elements` is populated from `selectionProvider` during resolution). Then implement `SourceEvaluator.java`, update `QueryEngine.execute()`, and write `QueryEngineExecutionTest.java`. Run build and tests, then create `03-02-SUMMARY.md`.

Command to execute plan: the execute-phase orchestrator should spawn a `gsd-executor` subagent for plan `03-02` (wave 2, since 03-01 is done).
</next_action>
